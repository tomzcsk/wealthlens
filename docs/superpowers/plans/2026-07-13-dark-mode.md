# F46 — Dark Mode Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** เปิดโหมดมืดได้ทั้งแอปผ่านชั้น token กลาง โดยโหมดสว่างต้องเหมือนเดิมทุกพิกเซล

**Architecture:** ค่าสีจริงอยู่ใน `src/index.css` ที่เดียว (เก็บเป็น **เลขช่องสี** ไม่ใช่ hex เพื่อให้ `bg-card/60` ทำงาน) → `tailwind.config.js` ประกาศชื่อ token ที่ชี้ไป `rgb(var(--x) / <alpha-value>)` → component อ้างชื่อ token เท่านั้น. โหมดสลับด้วย class `dark` บน `<html>` (`darkMode: 'class'`) ซึ่ง inline script ใน `index.html` ตั้งให้ตั้งแต่ก่อน React ตื่น (กันแฟลชขาว). กราฟ Recharts รับสีเป็น **ค่า** ไม่ใช่ class จึงมี `chartTheme.ts` pure จ่าย 5 ค่าตามโหมด

**Tech Stack:** React 18 + Vite + Tailwind (JIT, `darkMode: 'class'`) + Zustand persist + Recharts. verify script รันด้วย `npx tsx --tsconfig tsconfig.app.json scripts/<file>.ts` (แพทเทิร์นเดิมของโปรเจกต์). UI run ผ่าน Chrome (`npm run dev` → localhost:5173) กับข้อมูลจริงของ Tom

**Spec:** `docs/superpowers/specs/2026-07-13-dark-mode-design.md`

---

## กฎเหล็ก 2 ข้อ (ทุก task ต้องไม่ละเมิด)

1. **โหมดสว่างเหมือนเดิมทุกพิกเซล** — วิธีพิสูจน์ไม่ใช่ตาเปล่า แต่คือ **`verify-theme.ts` บังคับว่าค่า token โหมดสว่างทุกตัวต้องเท่ากับค่า Tailwind เดิมที่มันไปแทนเป๊ะ ๆ** (`ink-500` ต้อง = `#64748b` = `slate-500`) บวกกับ codemod ที่แทนชื่อ class แบบ 1:1 → โหมดสว่างเหมือนเดิม **โดยโครงสร้าง** ไม่ใช่โดยบังเอิญ
2. **ไม่ปรับดีไซน์** — งานนี้เปลี่ยนชั้นสี ไม่ใช่ปรับหน้าตา เจออะไรน่าปรับให้จดไว้ อย่าแก้

---

## File Structure

**สร้างใหม่**
| ไฟล์ | หน้าที่ |
|---|---|
| `src/lib/theme.ts` | pure: `ThemeMode`/`Resolved`, `resolveTheme()`, `cycleTheme()` — ไม่ import React/Zustand/DOM |
| `src/lib/chartTheme.ts` | pure: `chartPalette(resolved)` → 5 ค่าที่ Recharts ต้องการ |
| `src/stores/themeStore.ts` | Zustand + persist (key `wealthlens-theme`) + ผลข้างเคียง DOM (`<html>.classList`) + ฟัง `matchMedia` |
| `src/hooks/useChartTheme.ts` | hook บาง ๆ: อ่านโหมดที่ resolve แล้ว → คืน `chartPalette` |
| `src/components/layout/ThemeToggle.tsx` | ปุ่มสามจังหวะ ☀️ / 🌙 / 💻 |
| `scripts/codemod-theme-classes.mjs` | แทนชื่อ class แบบ 1:1 ตามตารางใน Task 5 (รับ path เป็น argument) |
| `scripts/verify-theme.ts` | pure helpers + parse `index.css` จริง → บังคับ R1–R4 + light-identity |
| `scripts/verify-no-hardcoded-colors.ts` | grep gate: class สีดิบต้องเหลือ 0 นอก allowlist |

**แก้**
| ไฟล์ | แก้อะไร |
|---|---|
| `src/index.css` | `:root` + `.dark` ถือค่าจริงทั้งหมดเป็นเลขช่องสี; ลบบล็อก `@media > :root.dark` ที่ตายอยู่ |
| `tailwind.config.js` | `darkMode: 'class'` + token ทั้งหมดชี้ `rgb(var(--x) / <alpha-value>)` |
| `index.html` | inline script กันแฟลชขาว |
| `src/components/layout/Header.tsx` | เสียบ `<ThemeToggle />` ก่อน `<div data-slot="auth">` |
| 4 ไฟล์กราฟ | เลิก hardcode → `useChartTheme()` |
| ~70 ไฟล์ component | codemod ชื่อ class |
| `src/pages/PrintReportPage.tsx` | บังคับสว่าง (ถอด class `dark` ตอน mount) |
| `src/pages/LoginPage.tsx` | gradient คู่มืด |
| `src/components/accounts/BankAvatar.tsx` | chip ขาวค้างไว้ตั้งใจ + comment |
| `src/components/ui/Modal.tsx` | `bg-slate-900/50` → `bg-overlay/50` |

---

## Task 1: `theme.ts` — ตัวตัดสินโหมด (pure)

**Files:**
- Create: `src/lib/theme.ts`
- Create: `scripts/verify-theme.ts`

- [ ] **Step 1: เขียน verify ที่ยังแดง**

`scripts/verify-theme.ts`:

```ts
/**
 * Verification for F46 — theme layer.
 *   npx tsx --tsconfig tsconfig.app.json scripts/verify-theme.ts
 */
import { cycleTheme, resolveTheme, type ThemeMode } from '../src/lib/theme';

let failures = 0;
const check = (label: string, actual: unknown, expected: unknown): void => {
  const ok = actual === expected;
  if (!ok) failures += 1;
  console.log(`${ok ? '✅' : '❌'} ${label}${ok ? '' : ` — ได้ ${String(actual)} ควรเป็น ${String(expected)}`}`);
};

console.log('\n— resolveTheme: เมทริกซ์ 3 โหมด × 2 สถานะเครื่อง —');
check('light + เครื่องสว่าง → light', resolveTheme('light', false), 'light');
check('light + เครื่องมืด → light', resolveTheme('light', true), 'light');
check('dark + เครื่องสว่าง → dark', resolveTheme('dark', false), 'dark');
check('dark + เครื่องมืด → dark', resolveTheme('dark', true), 'dark');
check('system + เครื่องสว่าง → light', resolveTheme('system', false), 'light');
check('system + เครื่องมืด → dark', resolveTheme('system', true), 'dark');

console.log('\n— cycleTheme: วนครบวงกลับที่เดิม —');
check('system → light', cycleTheme('system'), 'light');
check('light → dark', cycleTheme('light'), 'dark');
check('dark → system', cycleTheme('dark'), 'system');
const round: ThemeMode = cycleTheme(cycleTheme(cycleTheme('system')));
check('วนสามครั้งกลับที่เดิม', round, 'system');

console.log(failures === 0 ? '\n✅ ผ่านทั้งหมด' : `\n❌ ล้มเหลว ${failures} ข้อ`);
process.exit(failures === 0 ? 0 : 1);
```

- [ ] **Step 2: รันให้เห็นว่าแดง**

Run: `npx tsx --tsconfig tsconfig.app.json scripts/verify-theme.ts`
Expected: พังตอน import — `Cannot find module '../src/lib/theme'`

- [ ] **Step 3: เขียน `src/lib/theme.ts`**

```ts
/**
 * WealthLens — ชั้นตัดสินโหมดสี (F46).
 *
 * pure ล้วน: ไม่ import React / Zustand / DOM — ทดสอบใน node ได้ตรง ๆ
 * (หลักเดียวกับ lib/motion.ts และ utils/actionMessages.ts)
 *
 * ThemeMode = สิ่งที่ผู้ใช้เลือก · Resolved = สิ่งที่จอเห็นจริง
 * แยกสองคำนี้ให้ขาด เพราะ 'system' ไม่ใช่สี มันคือ 'ไปถามเครื่องเอา'
 */

export type ThemeMode = 'light' | 'dark' | 'system';
export type Resolved = 'light' | 'dark';

/** โหมดเริ่มต้นเมื่อยังไม่เคยเลือก — เคารพการตั้งค่าเครื่องก่อนเสมอ */
export const DEFAULT_MODE: ThemeMode = 'system';

/** key ใน LocalStorage — ธีมเป็นของเครื่อง ไม่ปนกับข้อมูลการเงิน */
export const THEME_STORAGE_KEY = 'wealthlens-theme';

export const resolveTheme = (
  mode: ThemeMode,
  systemPrefersDark: boolean,
): Resolved => {
  if (mode === 'system') return systemPrefersDark ? 'dark' : 'light';
  return mode;
};

/** ลำดับปุ่ม: ตามเครื่อง → สว่าง → มืด → ตามเครื่อง */
export const cycleTheme = (mode: ThemeMode): ThemeMode => {
  if (mode === 'system') return 'light';
  if (mode === 'light') return 'dark';
  return 'system';
};
```

- [ ] **Step 4: รันให้เขียว**

Run: `npx tsx --tsconfig tsconfig.app.json scripts/verify-theme.ts`
Expected: `✅ ผ่านทั้งหมด` (10 ข้อ) exit 0

- [ ] **Step 5: commit**

```bash
git add src/lib/theme.ts scripts/verify-theme.ts
git commit -m "feat(theme): ตัวตัดสินโหมดสี (pure) + verify (F46)"
```

---

## Task 2: ชั้น token — `index.css` + `tailwind.config.js`

ค่าทั้งหมดตัดสินแล้วใน spec §3.4–3.7 **ห้ามคิดค่าใหม่เอง**

**Files:**
- Modify: `src/index.css`
- Modify: `tailwind.config.js`
- Modify: `scripts/verify-theme.ts` (เพิ่มการตรวจ token)

- [ ] **Step 1: เพิ่มการตรวจ token ลง `scripts/verify-theme.ts` (ต่อท้ายก่อนบรรทัดสรุป)**

```ts
import { readFileSync } from 'node:fs';
import { resolve } from 'node:path';

// ---------- อ่านค่าจริงจาก index.css (ไม่ใช่ค่าที่คัดลอกมาไว้ในเทสต์) ----------
const css = readFileSync(resolve('src/index.css'), 'utf8');

const blockOf = (selector: string): string => {
  const i = css.indexOf(selector);
  if (i === -1) throw new Error(`หา selector ${selector} ใน index.css ไม่เจอ`);
  return css.slice(i, css.indexOf('}', i));
};

/** '--ink-500: 100 116 139;' → { 'ink-500': '100 116 139' } */
const tokensIn = (block: string): Record<string, string> => {
  const out: Record<string, string> = {};
  for (const [, name, value] of block.matchAll(/--([\w-]+):\s*([^;]+);/g)) {
    out[name] = value.trim();
  }
  return out;
};

const LIGHT = tokensIn(blockOf(':root {'));
const DARK = tokensIn(blockOf('.dark {'));

const hexOf = (channels: string): string =>
  '#' +
  channels
    .split(/\s+/)
    .map((n) => Number(n).toString(16).padStart(2, '0'))
    .join('');

const luminance = (hex: string): number => {
  const ch = [1, 3, 5]
    .map((i) => parseInt(hex.slice(i, i + 2), 16) / 255)
    .map((v) => (v <= 0.03928 ? v / 12.92 : ((v + 0.055) / 1.055) ** 2.4));
  return 0.2126 * ch[0] + 0.7152 * ch[1] + 0.0722 * ch[2];
};

const contrast = (a: string, b: string): number => {
  const [hi, lo] = [luminance(a), luminance(b)].sort((x, y) => y - x);
  return (hi + 0.05) / (lo + 0.05);
};

const assert = (label: string, ok: boolean, detail = ''): void => {
  if (!ok) failures += 1;
  console.log(`${ok ? '✅' : '❌'} ${label}${ok || !detail ? '' : ` — ${detail}`}`);
};

// ---------- R0: โหมดสว่างต้องเท่าค่า Tailwind เดิมเป๊ะ (กฎเหล็กข้อ 1) ----------
// นี่คือหลักฐานว่าโหมดสว่างไม่ขยับ: token ที่ไปแทน slate-500 ต้องเป็น slate-500 จริง ๆ
const LIGHT_MUST_EQUAL: Record<string, string> = {
  'ink-900': '#0f172a', // slate-900
  'ink-800': '#1e293b', // slate-800
  'ink-700': '#334155', // slate-700
  'ink-600': '#475569', // slate-600
  'ink-500': '#64748b', // slate-500
  'ink-400': '#94a3b8', // slate-400
  'ink-300': '#cbd5e1', // slate-300
  'ink-200': '#e2e8f0', // slate-200
  'ink-100': '#f1f5f9', // slate-100
  'bg-card': '#ffffff', // white
  'bg-surface': '#f8fafc', // slate-50
  'bg-hover': '#f8fafc', // slate-50 (hover บนการ์ดขาว)
  'bg-raised': '#f1f5f9', // slate-100
  'bg-track': '#e2e8f0', // slate-200
  'color-income-light': '#ecfdf5', // emerald-50
  'color-expense-light': '#fef2f2', // red-50
  'color-warning-light': '#fffbeb', // amber-50
  'color-primary-light': '#eff6ff', // blue-50
};

console.log('\n— R0: โหมดสว่างต้องเท่าค่าเดิมเป๊ะ —');
for (const [token, expected] of Object.entries(LIGHT_MUST_EQUAL)) {
  const actual = LIGHT[token] ? hexOf(LIGHT[token]) : '(ไม่มี)';
  assert(`${token} = ${expected}`, actual === expected, `ได้ ${actual}`);
}

// ---------- R1: token ทุกตัวมีค่าครบสองโหมด ----------
console.log('\n— R1: token ครบสองโหมด —');
for (const token of Object.keys(LIGHT)) {
  if (token.startsWith('cat-')) continue; // สีหมวดกราฟ ค่าเดียวสองโหมดโดยตั้งใจ
  assert(`${token} มีค่าโหมดมืด`, DARK[token] !== undefined);
}

// ---------- R2–R4: contrast ----------
const TEXT_TIERS = ['ink-900', 'ink-800', 'ink-700', 'ink-600', 'ink-500'];
const FAINT_TIER = ['ink-400'];
const LINE_TIERS = ['ink-300', 'ink-200', 'ink-100'];
const ACCENTS = ['color-income', 'color-expense', 'color-primary'];
const SURFACES = ['bg-card', 'bg-surface'];

const ratio = (map: Record<string, string>, fg: string, bg: string): number =>
  contrast(hexOf(map[fg]), hexOf(map[bg]));

console.log('\n— R2: ชั้นเนื้อความ ≥ 4.5 —');
for (const mode of [
  { name: 'สว่าง', map: LIGHT },
  { name: 'มืด', map: DARK },
]) {
  for (const fg of TEXT_TIERS) {
    for (const bg of SURFACES) {
      const r = ratio(mode.map, fg, bg);
      assert(`${mode.name}: ${fg} บน ${bg} = ${r.toFixed(2)}`, r >= 4.5);
    }
  }
}

console.log('\n— R3: ชั้นจาง ≥ 2.4 · ชั้นเส้น > 1.05 —');
for (const mode of [
  { name: 'สว่าง', map: LIGHT },
  { name: 'มืด', map: DARK },
]) {
  for (const bg of SURFACES) {
    for (const fg of FAINT_TIER) {
      const r = ratio(mode.map, fg, bg);
      assert(`${mode.name}: ${fg} บน ${bg} = ${r.toFixed(2)}`, r >= 2.4);
    }
    for (const fg of LINE_TIERS) {
      const r = ratio(mode.map, fg, bg);
      // ชั้นเส้นที่ contrast = 1.00 คือเส้นที่หายสนิทไปกับพื้น
      // เกณฑ์ 1.02 ไม่ใช่ 1.05 เพราะของเดิมเองก็คาบเส้น:
      // ink-100 บน bg-surface โหมดสว่าง = 1.052 (slate-100 บน slate-50)
      assert(`${mode.name}: ${fg} บน ${bg} = ${r.toFixed(3)} (ต้องเห็น)`, r > 1.02);
    }
  }
}

console.log('\n— R3b: accent ในโหมดมืด ≥ 4.5 บนการ์ด —');
for (const fg of ACCENTS) {
  const r = contrast(hexOf(DARK[fg]), hexOf(DARK['bg-card']));
  assert(`มืด: ${fg} = ${r.toFixed(2)}`, r >= 4.5);
}

console.log('\n— R4: ที่สว่างต่ำกว่า 7:1 → มืดต้องไม่แย่กว่าสว่าง —');
for (const fg of [...TEXT_TIERS, ...FAINT_TIER, ...LINE_TIERS]) {
  for (const bg of SURFACES) {
    const l = ratio(LIGHT, fg, bg);
    if (l >= 7) continue; // เหลือเฟือแล้ว ไม่ต้องคุม
    const d = ratio(DARK, fg, bg);
    assert(
      `${fg} บน ${bg}: มืด ${d.toFixed(2)} ≥ สว่าง ${l.toFixed(2)}`,
      d >= l,
    );
  }
}
```

- [ ] **Step 2: รันให้เห็นว่าแดง**

Run: `npx tsx --tsconfig tsconfig.app.json scripts/verify-theme.ts`
Expected: โยน `หา selector .dark { ใน index.css ไม่เจอ`

- [ ] **Step 3: เขียน `src/index.css` ใหม่ทั้งบล็อกสี**

แทนที่ทุกอย่างตั้งแต่ `:root {` ถึงท้ายบล็อก `@media (prefers-color-scheme: dark) { :root.dark {...} }` (บล็อกหลังนี้ **ลบทิ้ง** — มันตายมาแต่แรก) ด้วย:

```css
/*
 * ชั้นสีของทั้งแอป (F46) — แหล่งความจริงเดียว
 *
 * เก็บเป็น "เลขช่องสี" ไม่ใช่ hex เพราะ Tailwind ต้องประกอบ alpha เอง:
 *   rgb(var(--bg-card) / <alpha-value>)  →  bg-card/60 ถึงจะทำงาน
 * ถ้าเก็บเป็น #ffffff แล้วเขียน bg-card/60 มันจะเงียบและไม่ทำงาน
 *
 * โหมดสว่าง = ค่า Tailwind เดิมเป๊ะทุกตัว (scripts/verify-theme.ts บังคับไว้)
 * โหมดมืดเลือกค่าแล้ววัด contrast จริง ไม่ใช่กลับด้านตรง ๆ
 */
:root {
  /* ramp กลาง — ใช้ทั้ง text และ border (ค่าสว่าง = slate เดิม) */
  --ink-900: 15 23 42;
  --ink-800: 30 41 59;
  --ink-700: 51 65 85;
  --ink-600: 71 85 105;
  --ink-500: 100 116 139;
  --ink-400: 148 163 184;
  --ink-300: 203 213 225;
  --ink-200: 226 232 240;
  --ink-100: 241 245 249;

  /* พื้น */
  --bg-card: 255 255 255;
  --bg-surface: 248 250 252;
  --bg-hover: 248 250 252;
  --bg-raised: 241 245 249;
  --bg-track: 226 232 240;
  --bg-overlay: 15 23 42; /* ฉากหลัง Modal — มืดทั้งสองโหมดโดยตั้งใจ */

  /* accent */
  --color-primary: 37 99 235;
  --color-primary-light: 239 246 255;
  --color-primary-dark: 29 78 216;
  --color-income: 5 150 105;
  --color-income-light: 236 253 245;
  --color-income-bar: 52 211 153;
  --color-expense: 220 38 38;
  --color-expense-light: 254 242 242;
  --color-expense-bar: 248 113 113;
  --color-warning: 217 119 6;
  --color-warning-light: 255 251 235;
  --color-net: 124 58 237;
  --color-savings: 217 119 6;
  --color-success: 5 150 105;
  --color-danger: 220 38 38;
  --color-info: 37 99 235;

  /* สีหมวดกราฟ — ค่าเดียวสองโหมด (สดพอจะอ่านออกทั้งสองพื้น) */
  --cat-housing: 99 102 241;
  --cat-vehicle: 139 92 246;
  --cat-utilities: 6 182 212;
  --cat-subscription: 245 158 11;
  --cat-finance: 239 68 68;
  --cat-entertainment: 236 72 153;
  --cat-savings: 16 185 129;
  --cat-other: 107 114 128;

  font-synthesis: none;
  text-rendering: optimizeLegibility;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

.dark {
  --ink-900: 248 250 252;
  --ink-800: 241 245 249;
  --ink-700: 226 232 240;
  --ink-600: 203 213 225;
  --ink-500: 148 163 184;
  --ink-400: 100 116 139;
  --ink-300: 71 85 105;
  --ink-200: 51 65 85;
  --ink-100: 40 53 74; /* ไม่ใช่ 30 41 59 — นั่นเท่าสีการ์ดพอดี เส้นจะหายสนิท */

  --bg-card: 30 41 59;
  --bg-surface: 15 23 42;
  --bg-hover: 51 65 85; /* hover บนการ์ดต้องสว่างขึ้น ไม่ใช่มืดลง */
  --bg-raised: 51 65 85;
  --bg-track: 71 85 105;
  --bg-overlay: 15 23 42;

  /* accent สว่างขึ้นให้ contrast ผ่านบนพื้นมืด */
  --color-primary: 96 165 250;
  --color-primary-light: 30 58 138;
  --color-primary-dark: 147 197 253;
  --color-income: 52 211 153;
  --color-income-light: 6 78 59;
  --color-income-bar: 52 211 153;
  --color-expense: 248 113 113;
  --color-expense-light: 127 29 29;
  --color-expense-bar: 248 113 113;
  --color-warning: 251 191 36;
  --color-warning-light: 120 53 15;
  --color-net: 167 139 250;
  --color-savings: 251 191 36;
  --color-success: 52 211 153;
  --color-danger: 248 113 113;
  --color-info: 96 165 250;
}
```

> `--color-*-light` ในโหมดมืดเป็นสีเข้ม — ตั้งใจ. มันคือ "พื้น tint ของ badge" ไม่ใช่ "สีอ่อน"; component เรียกมันผ่าน `bg-income-light` แล้วได้พื้นเขียวเข้มในโหมดมืด ถูกต้องแล้ว. ความทึบคุมด้วย `/40` ที่ปลายทางเมื่อจำเป็น

- [ ] **Step 4: `tailwind.config.js` — `darkMode` + token**

แทนที่บล็อก `colors: {...}` ทั้งก้อนด้วย (และเพิ่ม `darkMode: 'class'` ระดับบนสุด):

```js
export default {
  darkMode: 'class',
  content: ['./index.html', './src/**/*.{js,ts,jsx,tsx}'],
  theme: {
    extend: {
      colors: {
        // ramp กลาง (text + border) — F46
        ink: {
          900: 'rgb(var(--ink-900) / <alpha-value>)',
          800: 'rgb(var(--ink-800) / <alpha-value>)',
          700: 'rgb(var(--ink-700) / <alpha-value>)',
          600: 'rgb(var(--ink-600) / <alpha-value>)',
          500: 'rgb(var(--ink-500) / <alpha-value>)',
          400: 'rgb(var(--ink-400) / <alpha-value>)',
          300: 'rgb(var(--ink-300) / <alpha-value>)',
          200: 'rgb(var(--ink-200) / <alpha-value>)',
          100: 'rgb(var(--ink-100) / <alpha-value>)',
        },
        // พื้น
        card: 'rgb(var(--bg-card) / <alpha-value>)',
        surface: 'rgb(var(--bg-surface) / <alpha-value>)',
        hover: 'rgb(var(--bg-hover) / <alpha-value>)',
        raised: 'rgb(var(--bg-raised) / <alpha-value>)',
        track: 'rgb(var(--bg-track) / <alpha-value>)',
        overlay: 'rgb(var(--bg-overlay) / <alpha-value>)',
        // accent
        primary: {
          DEFAULT: 'rgb(var(--color-primary) / <alpha-value>)',
          light: 'rgb(var(--color-primary-light) / <alpha-value>)',
          dark: 'rgb(var(--color-primary-dark) / <alpha-value>)',
        },
        income: {
          DEFAULT: 'rgb(var(--color-income) / <alpha-value>)',
          light: 'rgb(var(--color-income-light) / <alpha-value>)',
          bar: 'rgb(var(--color-income-bar) / <alpha-value>)',
        },
        expense: {
          DEFAULT: 'rgb(var(--color-expense) / <alpha-value>)',
          light: 'rgb(var(--color-expense-light) / <alpha-value>)',
          bar: 'rgb(var(--color-expense-bar) / <alpha-value>)',
        },
        warning: {
          DEFAULT: 'rgb(var(--color-warning) / <alpha-value>)',
          light: 'rgb(var(--color-warning-light) / <alpha-value>)',
        },
        net: 'rgb(var(--color-net) / <alpha-value>)',
        savings: 'rgb(var(--color-savings) / <alpha-value>)',
        success: 'rgb(var(--color-success) / <alpha-value>)',
        danger: 'rgb(var(--color-danger) / <alpha-value>)',
        info: 'rgb(var(--color-info) / <alpha-value>)',
        cat: {
          housing: 'rgb(var(--cat-housing) / <alpha-value>)',
          vehicle: 'rgb(var(--cat-vehicle) / <alpha-value>)',
          utilities: 'rgb(var(--cat-utilities) / <alpha-value>)',
          subscription: 'rgb(var(--cat-subscription) / <alpha-value>)',
          finance: 'rgb(var(--cat-finance) / <alpha-value>)',
          entertainment: 'rgb(var(--cat-entertainment) / <alpha-value>)',
          savings: 'rgb(var(--cat-savings) / <alpha-value>)',
          other: 'rgb(var(--cat-other) / <alpha-value>)',
        },
      },
      // fontFamily / fontSize เดิม — ห้ามแตะ
    },
  },
  plugins: [],
};
```

- [ ] **Step 5: รันให้เขียว**

Run: `npx tsx --tsconfig tsconfig.app.json scripts/verify-theme.ts`
Expected: `✅ ผ่านทั้งหมด` — R0 (18 ข้อ) + R1 + R2/R3/R4 เขียวหมด exit 0

Run: `npm run build`
Expected: build ผ่าน (Tailwind ยังคอมไพล์ token ใหม่ได้)

- [ ] **Step 6: commit**

```bash
git add src/index.css tailwind.config.js scripts/verify-theme.ts
git commit -m "feat(theme): ชั้น token — index.css เก็บเลขช่องสี + tailwind darkMode:class (F46)"
```

---

## Task 3: `themeStore` + inline script + ปุ่มสลับ

**Files:**
- Create: `src/stores/themeStore.ts`
- Create: `src/components/layout/ThemeToggle.tsx`
- Modify: `index.html`
- Modify: `src/components/layout/Header.tsx`

- [ ] **Step 1: `src/stores/themeStore.ts`**

```ts
/**
 * WealthLens — โหมดสี (F46).
 *
 * ธีมเป็นของ "เครื่อง" ไม่ใช่ของ "ผู้ใช้" จึงอยู่ใน LocalStorage key แยก
 * และ **ไม่ sync ขึ้น Drive** โดยตั้งใจ — มือถือกลางคืนอยากมืด เดสก์ท็อป
 * กลางวันอยากสว่าง ถ้า sync ข้ามเครื่องมันจะแย่งกันเปลี่ยน และมันไม่ใช่
 * ข้อมูลการเงิน จึงไม่ควรอยู่ใน wealthlens_data.json
 *
 * ผลข้างเคียง DOM (class บน <html>) อยู่ที่นี่ที่เดียว — component ไม่แตะ
 * classList เอง. ค่าเริ่มต้นถูกตั้งไว้แล้วโดย inline script ใน index.html
 * (กันแฟลชขาว) store แค่ "ยืนยัน" ค่าเดิมตอน hydrate ไม่ได้เปลี่ยนมัน
 */
import { create } from 'zustand';
import { persist } from 'zustand/middleware';

import {
  DEFAULT_MODE,
  THEME_STORAGE_KEY,
  cycleTheme,
  resolveTheme,
  type Resolved,
  type ThemeMode,
} from '@/lib/theme';

const DARK_QUERY = '(prefers-color-scheme: dark)';

const systemPrefersDark = (): boolean =>
  typeof window !== 'undefined' && window.matchMedia(DARK_QUERY).matches;

const paint = (resolved: Resolved): void => {
  if (typeof document === 'undefined') return;
  document.documentElement.classList.toggle('dark', resolved === 'dark');
};

interface ThemeState {
  mode: ThemeMode;
  /** สิ่งที่จอเห็นจริง — derive จาก mode + เครื่อง, อัปเดตเมื่อ OS สลับ */
  resolved: Resolved;
  setMode: (mode: ThemeMode) => void;
  toggle: () => void;
  /** เรียกครั้งเดียวตอน mount — ผูก listener ของ OS, คืน unsubscribe */
  syncWithSystem: () => () => void;
}

export const useThemeStore = create<ThemeState>()(
  persist(
    (set, get) => ({
      mode: DEFAULT_MODE,
      resolved: resolveTheme(DEFAULT_MODE, systemPrefersDark()),

      setMode: (mode) => {
        const resolved = resolveTheme(mode, systemPrefersDark());
        paint(resolved);
        set({ mode, resolved });
      },

      toggle: () => get().setMode(cycleTheme(get().mode)),

      syncWithSystem: () => {
        if (typeof window === 'undefined') return () => {};
        const mq = window.matchMedia(DARK_QUERY);
        const onChange = (e: MediaQueryListEvent): void => {
          // เคารพการเลือกของผู้ใช้: เครื่องเปลี่ยนมีผลเฉพาะโหมด 'system'
          if (get().mode !== 'system') return;
          const resolved: Resolved = e.matches ? 'dark' : 'light';
          paint(resolved);
          set({ resolved });
        };
        mq.addEventListener('change', onChange);
        return () => mq.removeEventListener('change', onChange);
      },
    }),
    {
      name: THEME_STORAGE_KEY,
      partialize: (s) => ({ mode: s.mode }), // resolved คำนวณใหม่เสมอ ไม่เก็บ
      onRehydrateStorage: () => (state) => {
        if (!state) return;
        const resolved = resolveTheme(state.mode, systemPrefersDark());
        paint(resolved);
        state.resolved = resolved;
      },
    },
  ),
);
```

- [ ] **Step 2: inline script ใน `index.html`** — วางใน `<head>` **ก่อน** `<script type="module">`

```html
    <script>
      // F46 — ตั้งโหมดก่อน React ตื่น ไม่งั้นเห็นแฟลชขาวทุกครั้งที่ refresh
      // ในโหมดมืด. ต้องอยู่ใน head และต้องเป็น sync script
      // localStorage ถูกปิดได้ (โหมดส่วนตัวบางเบราว์เซอร์) — ธีมพังต้องไม่ทำให้แอปไม่ขึ้น
      try {
        var raw = localStorage.getItem('wealthlens-theme');
        var mode = raw ? JSON.parse(raw).state.mode : 'system';
        var dark =
          mode === 'dark' ||
          (mode === 'system' &&
            window.matchMedia('(prefers-color-scheme: dark)').matches);
        if (dark) document.documentElement.classList.add('dark');
      } catch (e) {
        /* ไม่มีธีมที่จำไว้ → สว่าง */
      }
    </script>
```

- [ ] **Step 3: `src/components/layout/ThemeToggle.tsx`**

```tsx
/**
 * WealthLens — ปุ่มสลับโหมดสี (F46).
 * สามจังหวะ: ตามเครื่อง → สว่าง → มืด
 */
import { useEffect, type ReactNode } from 'react';

import type { ThemeMode } from '@/lib/theme';
import { useThemeStore } from '@/stores/themeStore';

const FACE: Record<ThemeMode, { icon: string; label: string }> = {
  system: { icon: '💻', label: 'ตามเครื่อง' },
  light: { icon: '☀️', label: 'สว่าง' },
  dark: { icon: '🌙', label: 'มืด' },
};

export const ThemeToggle = (): ReactNode => {
  const mode = useThemeStore((s) => s.mode);
  const toggle = useThemeStore((s) => s.toggle);
  const syncWithSystem = useThemeStore((s) => s.syncWithSystem);

  useEffect(() => syncWithSystem(), [syncWithSystem]);

  const face = FACE[mode];

  return (
    <button
      type="button"
      onClick={toggle}
      title={`โหมด: ${face.label}`}
      aria-label={`โหมดสี: ${face.label} — กดเพื่อเปลี่ยน`}
      className="inline-flex items-center justify-center w-9 h-9 rounded-lg border border-ink-200 bg-card text-base hover:bg-hover transition motion-safe:active:scale-[0.98]"
    >
      <span aria-hidden="true">{face.icon}</span>
    </button>
  );
};

export default ThemeToggle;
```

- [ ] **Step 4: เสียบปุ่มใน `Header.tsx`**

import: `import ThemeToggle from './ThemeToggle';`
แล้วแทรกก่อน `<div data-slot="auth" ...>`:

```jsx
          <ThemeToggle />

          <div data-slot="auth" className="flex items-center gap-2">
```

- [ ] **Step 5: ขับดูจริง**

Run: `npm run dev` แล้วเปิด `http://localhost:5173`
Expected:
- ปุ่มโผล่ใน Header, กดแล้ววน 💻 → ☀️ → 🌙
- โหมดมืด: **พื้นหน้าเปลี่ยนเป็นเข้ม แต่การ์ดยังขาว** ← ถูกต้อง ยังไม่ได้แปลง component (Task 5)
- refresh แล้วโหมดคงเดิม + **ไม่มีแฟลชขาว**
- `document.documentElement.className` มี `dark` เมื่อโหมดมืด

- [ ] **Step 6: commit**

```bash
git add src/stores/themeStore.ts src/components/layout/ThemeToggle.tsx src/components/layout/Header.tsx index.html
git commit -m "feat(theme): store + ปุ่มสามจังหวะ + inline script กันแฟลชขาว (F46)"
```

---

## Task 4: กราฟ — `chartTheme.ts` + `useChartTheme()`

กราฟรับสีเป็น **ค่า** ไม่ใช่ class → codemod ช่วยไม่ได้
สำรวจแล้ว: ไม่มี `contentStyle`/`labelStyle`/`itemStyle` ที่ไหนเลย (tooltip ทุกตัวเป็น component ของเราเองที่ใช้ Tailwind class → ได้ dark ฟรีจาก Task 5) เหลือต้องจ่ายแค่ **5 ค่า**

**Files:**
- Create: `src/lib/chartTheme.ts`
- Create: `src/hooks/useChartTheme.ts`
- Modify: `src/components/dashboard/IncomeExpenseChart.tsx`
- Modify: `src/components/analytics/TrendAnalysis.tsx`
- Modify: `src/components/analytics/MultiYearComparison.tsx`
- Modify: `scripts/verify-theme.ts`

- [ ] **Step 1: เพิ่ม assertion ลง `scripts/verify-theme.ts` (ต่อท้าย)**

```ts
import { chartPalette } from '../src/lib/chartTheme';

console.log('\n— chartPalette: ครบทั้งสองโหมดและไม่ซ้ำกัน —');
const lightChart = chartPalette('light');
const darkChart = chartPalette('dark');
for (const key of ['grid', 'axisLine', 'axisTick', 'cursorFill', 'cursorStroke'] as const) {
  assert(`${key} มีค่าโหมดสว่าง`, Boolean(lightChart[key]));
  assert(`${key} มีค่าโหมดมืด`, Boolean(darkChart[key]));
}
// เส้นตารางสีอ่อนบนพื้นมืด = เรืองแสง — ต้องต่างกันจริง
assert('grid ต่างกันสองโหมด', lightChart.grid !== darkChart.grid);
assert('เส้นตารางสว่างคือ #E2E8F0 เดิม', lightChart.grid === '#E2E8F0');
```

- [ ] **Step 2: รันให้เห็นว่าแดง**

Run: `npx tsx --tsconfig tsconfig.app.json scripts/verify-theme.ts`
Expected: `Cannot find module '../src/lib/chartTheme'`

- [ ] **Step 3: `src/lib/chartTheme.ts`**

```ts
/**
 * WealthLens — สีของกราฟตามโหมด (F46).
 *
 * ทำไมต้องมีไฟล์นี้ ทั้งที่ทุกที่ในแอปใช้ token ผ่าน Tailwind:
 * Recharts รับสีเป็น **prop** แล้วยัดลงเป็น SVG presentation attribute
 * ซึ่งสเปกไม่รับ var() → stroke="var(--ink-200)" จะได้เส้นหายเงียบ ๆ
 * ไม่มี error ให้เห็น. จึงต้องส่ง hex ตรงตามโหมด
 *
 * สีซีรีส์ (income/expense/net) และสีหมวดทั้ง 8 ไม่ได้อยู่ในนี้โดยตั้งใจ —
 * มันสดพอจะอ่านออกทั้งสองพื้นอยู่แล้ว เปลี่ยนไปก็ได้แต่ไม่ได้อะไรเพิ่ม
 *
 * pure: ไม่ import React — ทดสอบใน node ได้
 */
import type { Resolved } from './theme';

export interface ChartPalette {
  /** เส้นตารางพื้นหลัง (CartesianGrid) */
  grid: string;
  /** เส้นแกน (XAxis/YAxis axisLine) */
  axisLine: string;
  /** ตัวหนังสือ/ขีดบนแกน (XAxis/YAxis stroke) */
  axisTick: string;
  /** แถบไฮไลต์ใต้เคอร์เซอร์ของกราฟแท่ง */
  cursorFill: string;
  /** เส้นประไฮไลต์ของกราฟเส้น/พื้นที่ */
  cursorStroke: string;
}

const LIGHT: ChartPalette = {
  grid: '#E2E8F0',
  axisLine: '#E2E8F0',
  axisTick: '#94A3B8',
  cursorFill: 'rgba(148, 163, 184, 0.08)',
  cursorStroke: '#94A3B8',
};

const DARK: ChartPalette = {
  grid: '#334155',
  axisLine: '#334155',
  axisTick: '#94A3B8', // อ่านออกทั้งสองพื้น จึงคงไว้
  cursorFill: 'rgba(148, 163, 184, 0.14)',
  cursorStroke: '#64748B',
};

export const chartPalette = (resolved: Resolved): ChartPalette =>
  resolved === 'dark' ? DARK : LIGHT;
```

- [ ] **Step 4: `src/hooks/useChartTheme.ts`**

```ts
/** WealthLens — สีกราฟตามโหมดที่กำลังแสดงอยู่ (F46). */
import { chartPalette, type ChartPalette } from '@/lib/chartTheme';
import { useThemeStore } from '@/stores/themeStore';

export const useChartTheme = (): ChartPalette =>
  chartPalette(useThemeStore((s) => s.resolved));

export default useChartTheme;
```

- [ ] **Step 5: แทนค่าใน 3 ไฟล์กราฟ**

`ExpensePieChart.tsx` **ไม่ต้องแตะ** — ไม่มีแกน ไม่มีตาราง (`stroke="none"` บน Pie เป็นค่าคงที่ ไม่ใช่สี)

ในแต่ละไฟล์: เพิ่ม `const chart = useChartTheme();` ในตัว component แล้วแทน:

| เดิม | ใหม่ |
|---|---|
| `<CartesianGrid ... stroke="#E2E8F0" />` | `stroke={chart.grid}` |
| `<XAxis ... stroke="#94A3B8" />` / `<YAxis ... stroke="#94A3B8" />` | `stroke={chart.axisTick}` |
| `axisLine={{ stroke: '#E2E8F0' }}` | `axisLine={{ stroke: chart.axisLine }}` |
| `cursor={{ fill: 'rgba(148, 163, 184, 0.08)' }}` | `cursor={{ fill: chart.cursorFill }}` |
| `cursor={{ stroke: '#94A3B8', strokeDasharray: '3 3' }}` | `cursor={{ stroke: chart.cursorStroke, strokeDasharray: '3 3' }}` |

จุดที่ต้องแตะ (จากการสำรวจ):
- `IncomeExpenseChart.tsx` — L208, 211, 214, 217, 221, 226
- `TrendAnalysis.tsx` — L306, 311, 314, 318, 322, 327 (กราฟพื้นที่) + L535, 540, 543, 547, 551, 556 (กราฟแท่งซ้อน)
- `MultiYearComparison.tsx` — L425, 430, 433, 436, 440, 445

**อย่าแตะ:** `COLOR_INCOME_BAR` / `COLOR_EXPENSE_BAR` / `COLOR_NET` / `CATEGORY_HEX_COLORS` / `YEAR_COLORS` / gradient stops — สีซีรีส์ใช้ได้ทั้งสองพื้น

- [ ] **Step 6: รันให้เขียว**

Run: `npx tsx --tsconfig tsconfig.app.json scripts/verify-theme.ts` → `✅ ผ่านทั้งหมด`
Run: `npm run typecheck` → ผ่าน

- [ ] **Step 7: commit**

```bash
git add src/lib/chartTheme.ts src/hooks/useChartTheme.ts src/components/dashboard/IncomeExpenseChart.tsx src/components/analytics/TrendAnalysis.tsx src/components/analytics/MultiYearComparison.tsx scripts/verify-theme.ts
git commit -m "feat(theme): กราฟรับสีตามโหมดผ่าน chartTheme (Recharts ใช้ var() ไม่ได้) (F46)"
```

---

## Task 5: codemod — ตารางแปลงชื่อ class

**Files:**
- Create: `scripts/codemod-theme-classes.mjs`

- [ ] **Step 1: เขียน codemod**

```js
/**
 * F46 — แทนชื่อ class สีดิบด้วย token (1:1 เท่านั้น)
 *   node scripts/codemod-theme-classes.mjs src/components/dashboard
 *
 * กฎ: การแทนต้องได้สีโหมดสว่าง "เท่าเดิมเป๊ะ" ทุกตัว
 * (verify-theme.ts R0 บังคับว่าค่า token โหมดสว่าง = ค่า slate เดิม)
 * ห้ามใส่การแทนที่เปลี่ยนสีโหมดสว่างลงตารางนี้เด็ดขาด
 */
import { readFileSync, writeFileSync, readdirSync, statSync } from 'node:fs';
import { join } from 'node:path';

// เรียงจากยาวไปสั้นสำคัญมาก: bg-slate-100 ต้องถูกแทนก่อน bg-slate-1 ไม่มีจริง
// แต่ hover:bg-slate-50 ต้องมาก่อน bg-slate-50 ไม่งั้นได้ hover:bg-surface ผิด
const RULES = [
  // --- พื้น: hover/focus/disabled มาก่อนตัวเปล่าเสมอ ---
  [/\bhover:bg-slate-50\b/g, 'hover:bg-hover'],
  [/\bfocus:bg-slate-50\b/g, 'focus:bg-hover'],
  [/\bdisabled:bg-slate-50\b/g, 'disabled:bg-hover'],
  [/\bhover:bg-slate-100\b/g, 'hover:bg-raised'],
  [/\bbg-slate-50\/(\d+)\b/g, 'bg-surface/$1'],
  [/\bbg-slate-50\b/g, 'bg-surface'],
  [/\bbg-slate-100\/(\d+)\b/g, 'bg-raised/$1'],
  [/\bbg-slate-100\b/g, 'bg-raised'],
  [/\bbg-slate-200\b/g, 'bg-track'],
  [/\bdisabled:bg-slate-300\b/g, 'disabled:bg-ink-300'],
  [/\bbg-slate-300\b/g, 'bg-ink-300'],
  [/\bbg-slate-900\/(\d+)\b/g, 'bg-overlay/$1'],
  [/\bbg-slate-900\b/g, 'bg-overlay'],
  [/\bbg-white\/(\d+)\b/g, 'bg-card/$1'],
  [/\bbg-white\b/g, 'bg-card'],

  // --- ตัวหนังสือ (ramp ตรงขั้นต่อขั้น) ---
  [/\bhover:text-slate-(\d00)\b/g, 'hover:text-ink-$1'],
  [/\bplaceholder:text-slate-(\d00)\b/g, 'placeholder:text-ink-$1'],
  [/\btext-slate-(\d00)\b/g, 'text-ink-$1'],

  // --- เส้นขอบ / เส้นคั่น ---
  [/\bhover:border-slate-(\d00)\b/g, 'hover:border-ink-$1'],
  [/\bborder-slate-(\d00)\b/g, 'border-ink-$1'],
  [/\bdivide-slate-(\d00)\b/g, 'divide-ink-$1'],

  // --- tint ของ badge ---
  [/\bbg-emerald-50\b/g, 'bg-income-light'],
  [/\bbg-emerald-100\b/g, 'bg-income-light'],
  [/\bbg-red-50\b/g, 'bg-expense-light'],
  [/\bbg-red-100\b/g, 'bg-expense-light'],
  [/\bbg-rose-50\b/g, 'bg-expense-light'],
  [/\bbg-amber-50\b/g, 'bg-warning-light'],
  [/\bbg-amber-100\b/g, 'bg-warning-light'],
  [/\bbg-blue-50\b/g, 'bg-primary-light'],
  [/\bbg-blue-100\b/g, 'bg-primary-light'],
];

const files = [];
const walk = (p) => {
  if (statSync(p).isDirectory()) {
    for (const e of readdirSync(p)) walk(join(p, e));
  } else if (p.endsWith('.tsx')) {
    files.push(p);
  }
};
for (const arg of process.argv.slice(2)) walk(arg);

let touched = 0;
for (const file of files) {
  const before = readFileSync(file, 'utf8');
  let after = before;
  for (const [pattern, replacement] of RULES) {
    after = after.replace(pattern, replacement);
  }
  if (after !== before) {
    writeFileSync(file, after);
    touched += 1;
    console.log(`✏️  ${file}`);
  }
}
console.log(`\nแก้ ${touched} ไฟล์ จาก ${files.length} ไฟล์`);
```

> **`bg-emerald-100` → `bg-income-light` เปลี่ยนสีโหมดสว่าง!** (`#d1fae5` → `#ecfdf5`) — 3 จุด. เช่นเดียวกับ `bg-amber-100` (6), `bg-red-100` (1), `bg-blue-100` (1), `bg-rose-50` (1) รวม **12 จุด** ที่ยอมให้สว่างขยับ. Tom อนุมัติกรณี `rose-50` (ต่างกัน ~1/255 ต่อช่องสี — ตาแยกไม่ออก) แล้ว. **แต่ `-100` → `-50` ต่างกันชัดกว่านั้น** → ดู Task 5b ก่อนรัน codemod

- [ ] **Step 2: commit**

```bash
git add scripts/codemod-theme-classes.mjs
git commit -m "chore(theme): codemod แปลงชื่อ class สีดิบเป็น token (F46)"
```

---

## Task 5b: จัดการ `-100` tint (12 จุด) — ก่อนรัน codemod

`-100` เข้มกว่า `-50` ชัดเจน ยุบเข้าหากันคือละเมิดกฎเหล็กข้อ 1

**Files:**
- Modify: `src/index.css`, `tailwind.config.js`, `scripts/codemod-theme-classes.mjs`, `scripts/verify-theme.ts`

- [ ] **Step 1: เพิ่ม tint ชั้นเข้มเป็น token ของตัวเอง**

`index.css` `:root` เพิ่ม:
```css
  --color-income-tint: 209 250 229;   /* emerald-100 */
  --color-expense-tint: 254 226 226;  /* red-100 */
  --color-warning-tint: 254 243 199;  /* amber-100 */
  --color-primary-tint: 219 234 254;  /* blue-100 */
```
`.dark` เพิ่ม (เข้มกว่า `-light` หนึ่งขั้นเพื่อคงลำดับชั้นเดิม):
```css
  --color-income-tint: 6 95 70;
  --color-expense-tint: 153 27 27;
  --color-warning-tint: 146 64 14;
  --color-primary-tint: 30 64 175;
```

`tailwind.config.js` เพิ่ม `tint:` ในแต่ละ accent:
```js
        income: { DEFAULT: ..., light: ..., tint: 'rgb(var(--color-income-tint) / <alpha-value>)', bar: ... },
```
(ทำเหมือนกันกับ `expense`, `warning`, `primary`)

- [ ] **Step 2: แก้ตาราง codemod — `-100` ไปที่ `tint` ไม่ใช่ `light`**

```js
  [/\bbg-emerald-100\b/g, 'bg-income-tint'],
  [/\bbg-red-100\b/g, 'bg-expense-tint'],
  [/\bbg-amber-100\b/g, 'bg-warning-tint'],
  [/\bbg-blue-100\b/g, 'bg-primary-tint'],
```
(ลบบรรทัด `-100 → -light` เดิมทิ้ง)

`bg-rose-50 → bg-expense-light` **คงไว้** — Tom อนุมัติแล้ว (ΔE มองไม่เห็น)

- [ ] **Step 3: เพิ่ม R0 assertion ของ tint ใหม่ใน `verify-theme.ts`**

เติมลง `LIGHT_MUST_EQUAL`:
```ts
  'color-income-tint': '#d1fae5',
  'color-expense-tint': '#fee2e2',
  'color-warning-tint': '#fef3c7',
  'color-primary-tint': '#dbeafe',
```

- [ ] **Step 4: รันให้เขียว**

Run: `npx tsx --tsconfig tsconfig.app.json scripts/verify-theme.ts`
Expected: `✅ ผ่านทั้งหมด` (R0 มี 22 ข้อแล้ว)

- [ ] **Step 5: commit**

```bash
git add src/index.css tailwind.config.js scripts/codemod-theme-classes.mjs scripts/verify-theme.ts
git commit -m "feat(theme): tint ชั้นเข้ม (emerald/amber/red/blue-100) เป็น token ของตัวเอง (F46)"
```

---

## Task 6–11: รัน codemod ทีละกลุ่ม

**ทำเหมือนกันทุกกลุ่ม** — แยก commit เพื่อให้ย้อนกลับได้ทีละกลุ่มถ้าเจอปัญหา

| Task | คำสั่ง |
|---|---|
| 6 | `node scripts/codemod-theme-classes.mjs src/components/ui src/components/layout src/components/motion src/components/ErrorBoundary.tsx src/App.tsx` |
| 7 | `node scripts/codemod-theme-classes.mjs src/components/dashboard` |
| 8 | `node scripts/codemod-theme-classes.mjs src/components/forms` |
| 9 | `node scripts/codemod-theme-classes.mjs src/components/accounts src/components/loans` |
| 10 | `node scripts/codemod-theme-classes.mjs src/components/analytics src/components/settings src/components/wealth src/components/auth` |
| 11 | `node scripts/codemod-theme-classes.mjs src/pages` |

แต่ละ Task ทำ 5 ขั้น:

- [ ] **Step 1: รัน codemod ตามคำสั่งของกลุ่มนั้น** — จดจำนวนไฟล์ที่แก้
- [ ] **Step 2: `npm run typecheck && npm run lint`** — ต้องผ่าน
- [ ] **Step 3: `git diff --stat`** — ดูว่าไม่มีไฟล์นอกกลุ่มถูกแตะ
- [ ] **Step 4: `git diff` แล้วอ่านทุกบรรทัด** — มองหา 2 อย่างเท่านั้น:
  - class ไหนที่ **ไม่ได้** แปลง แต่ควรแปลง (สีดิบตกค้าง)
  - class ไหนที่แปลงผิดขั้น (`text-slate-600` ต้องได้ `text-ink-600` ไม่ใช่ `700`)
- [ ] **Step 5: commit** — `git commit -am "refactor(theme): <ชื่อกลุ่ม> ใช้ token (F46)"`

> **Task 11 พิเศษ:** `src/pages` มี `PrintReportPage.tsx` (ห้ามแปลง) และ `LoginPage.tsx` (แปลงบางส่วน) → **ก่อนรัน codemod ให้ `git stash` ไว้ไม่ได้** — วิธีที่ถูกคือรัน codemod ทั้งโฟลเดอร์ตามปกติ แล้วใน Task 12 คืนค่า `PrintReportPage.tsx` ด้วย `git checkout HEAD~1 -- src/pages/PrintReportPage.tsx` (มันต้องคงสีดิบไว้)

---

## Task 12: เคสขอบ 4 จุด

**Files:**
- Modify: `src/pages/PrintReportPage.tsx`
- Modify: `src/pages/LoginPage.tsx`
- Modify: `src/components/accounts/BankAvatar.tsx`
- Modify: `src/components/ui/Modal.tsx`

- [ ] **Step 1: `PrintReportPage.tsx` — คืนสีดิบ + บังคับสว่าง**

```bash
git checkout HEAD~1 -- src/pages/PrintReportPage.tsx   # ย้อน codemod ของ Task 11 เฉพาะไฟล์นี้
```

แล้วเพิ่ม effect ในตัว component (ก่อน `return`):

```tsx
  // F46 — หน้ารายงานขาวเสมอ แม้แอปอยู่โหมดมืด: พิมพ์ลงกระดาษขาว พื้นดำ
  // กินหมึกและอ่านไม่ออก. ต้องถอด class ที่ <html> ไม่ใช่แค่ไม่แปลงไฟล์นี้ —
  // พื้น <body> มาจาก token จะยังมืดอยู่
  useEffect(() => {
    const root = document.documentElement;
    const wasDark = root.classList.contains('dark');
    root.classList.remove('dark');
    return () => {
      if (wasDark) root.classList.add('dark');
    };
  }, []);
```

(เพิ่ม `useEffect` ใน import ของ react ถ้ายังไม่มี)

- [ ] **Step 2: `LoginPage.tsx` — gradient คู่มืด**

```
- className="min-h-screen bg-gradient-to-br from-slate-50 via-blue-50 to-indigo-50 flex items-center justify-center px-4 py-12"
+ className="min-h-screen bg-gradient-to-br from-surface via-primary-light to-surface flex items-center justify-center px-4 py-12"
```
โลโก้ Google SVG (L113–125: `#FFC107`/`#FF3D00`/`#4CAF50`/`#1976D2`) **ห้ามแตะ** — สีแบรนด์

- [ ] **Step 3: `BankAvatar.tsx` — chip ขาวค้างไว้ตั้งใจ**

codemod เปลี่ยน `bg-white` เป็น `bg-card` ในกล่องโลโก้ไปแล้ว → **ย้อนกลับเป็น `bg-white`** และใส่ comment:

```tsx
  return (
    // F46 — bg-white ตรงนี้ตั้งใจ ไม่ใช่ของตกหล่น: โลโก้ธนาคารเป็น PNG สีเข้ม
    // (CITI/BBL) ถ้าพื้นเป็น bg-card มันจะจมหายไปในโหมดมืด. chip ขาวคงที่
    // ทั้งสองโหมด — เหมือนโลโก้พิมพ์บนกระดาษ
    <span
      className={`inline-flex shrink-0 items-center justify-center rounded-lg bg-white border border-ink-200 overflow-hidden ${box}`}
      title={bank.label}
    >
```
(ขอบเป็น `border-ink-200` ตามปกติ — ปรับตามโหมดได้ ไม่มีปัญหา)

- [ ] **Step 4: `Modal.tsx` — ตรวจว่าฉากหลังถูกแปลงเป็น `bg-overlay/50`**

codemod ควรเปลี่ยน `bg-slate-900/50` → `bg-overlay/50` ให้แล้ว ยืนยันด้วย `grep -n "bg-overlay" src/components/ui/Modal.tsx`

- [ ] **Step 5: typecheck + lint + build**

Run: `npm run typecheck && npm run lint && npm run build`
Expected: ผ่านทั้งสาม

- [ ] **Step 6: commit**

```bash
git add src/pages/PrintReportPage.tsx src/pages/LoginPage.tsx src/components/accounts/BankAvatar.tsx src/components/ui/Modal.tsx
git commit -m "feat(theme): เคสขอบ — PDF ขาวเสมอ, login gradient, chip โลโก้ธนาคาร (F46)"
```

---

## Task 13: ประตูกันถอยหลัง (`verify-no-hardcoded-colors.ts`)

**Files:**
- Create: `scripts/verify-no-hardcoded-colors.ts`

- [ ] **Step 1: เขียน gate**

```ts
/**
 * F46 — กันสีดิบกลับเข้ามาในอนาคต
 *   npx tsx --tsconfig tsconfig.app.json scripts/verify-no-hardcoded-colors.ts
 *
 * ไม่ได้มีไว้ตรวจงานวันนี้ (codemod ทำเสร็จแล้ว) — มีไว้ตรวจ component
 * ตัวถัดไปที่ใครสักคนเขียนด้วย bg-white แล้วมันขาวโพลนในโหมดมืด
 */
import { readFileSync, readdirSync, statSync } from 'node:fs';
import { join } from 'node:path';

/** ไฟล์ที่ "ต้อง" มีสีดิบ — พร้อมเหตุผล */
const ALLOWLIST: Record<string, string> = {
  'src/pages/PrintReportPage.tsx': 'รายงาน PDF ขาวเสมอ (พิมพ์ลงกระดาษ)',
  'src/components/accounts/BankAvatar.tsx': 'chip ขาวรองโลโก้ธนาคารสีเข้ม',
  'src/components/auth/GoogleSignInButton.tsx': 'สีแบรนด์ Google',
  'src/pages/LoginPage.tsx': 'โลโก้ Google (สีแบรนด์)',
};

const BANNED =
  /\b(?:hover:|focus:|active:|group-hover:|disabled:|placeholder:)?(?:bg|text|border|divide|ring)-(?:slate|gray)-\d00\b|\bbg-white\b(?!\/)/g;

const files: string[] = [];
const walk = (p: string): void => {
  if (statSync(p).isDirectory()) {
    for (const e of readdirSync(p)) walk(join(p, e));
  } else if (p.endsWith('.tsx')) {
    files.push(p);
  }
};
walk('src');

let violations = 0;
for (const file of files) {
  const normalized = file.replace(/\\/g, '/');
  if (ALLOWLIST[normalized]) continue;
  const lines = readFileSync(file, 'utf8').split('\n');
  lines.forEach((line, i) => {
    for (const hit of line.matchAll(BANNED)) {
      violations += 1;
      console.log(`❌ ${normalized}:${i + 1} — ${hit[0]}`);
    }
  });
}

if (violations === 0) {
  console.log(`✅ ไม่มีสีดิบนอก allowlist (${files.length} ไฟล์)`);
  console.log('\nไฟล์ที่ยกเว้นไว้โดยตั้งใจ:');
  for (const [f, why] of Object.entries(ALLOWLIST)) console.log(`   • ${f} — ${why}`);
} else {
  console.log(`\n❌ เจอสีดิบ ${violations} จุด — ใช้ token แทน (ดู src/index.css)`);
}
process.exit(violations === 0 ? 0 : 1);
```

- [ ] **Step 2: รัน**

Run: `npx tsx --tsconfig tsconfig.app.json scripts/verify-no-hardcoded-colors.ts`
Expected: `✅ ไม่มีสีดิบนอก allowlist`
ถ้าแดง → codemod ตกหล่น กลับไปแก้ที่ต้นเหตุ (เพิ่มกฎใน codemod แล้วรันซ้ำ) **อย่าเติม allowlist เพื่อให้เขียว**

- [ ] **Step 3: commit**

```bash
git add scripts/verify-no-hardcoded-colors.ts
git commit -m "test(theme): ประตูกันสีดิบกลับเข้ามา (F46)"
```

---

## Task 14: ตรวจของจริง + ปิดงาน

- [ ] **Step 1: verify ทั้งชุดต้องไม่ regress**

รันทุกไฟล์ใน `scripts/verify-*.ts` (28 ตัวรวมของใหม่):

```bash
for f in scripts/verify-*.ts; do
  echo "── $f"
  npx tsx --tsconfig tsconfig.app.json "$f" > /dev/null 2>&1 && echo "  ✅" || echo "  ❌ แดง"
done
```
Expected: เขียวทุกตัว

- [ ] **Step 2: `npm run typecheck && npm run lint && npm run build`** — ผ่านทั้งสาม

- [ ] **Step 3: ขับของจริงบน Chrome (`npm run dev`) กับข้อมูลจริงของ Tom**

ไล่ทุกหน้า **ทั้งสองโหมด**: `/` `/monthly` `/analytics` `/installments` `/loans` `/accounts` `/gold` `/wealth` `/tax` `/settings`

สิ่งที่ต้องเห็น:
- ไม่มีการ์ดขาวโพลนค้างในโหมดมืด, ไม่มีตัวหนังสือจมพื้น
- เส้นคั่นในตารางยังเห็นได้ในโหมดมืด (ทดสอบ `ink-100`)
- hover แถวตารางแล้ว **สว่างขึ้น** ไม่ใช่มืดลง
- กราฟ: เส้นตารางไม่เรืองแสง, tooltip พื้นเข้มตัวหนังสืออ่านออก
- badge (เบิกแล้ว / ค้าง / ผ่อน) พื้น tint เข้ม ไม่ใช่แผ่นสว่างแสบตา
- Modal เปิดมาพื้นเข้ม, ฉากหลังยังมืด
- โลโก้ธนาคารยังเห็นชัด (chip ขาว)
- `/report/2025` **ขาว** แม้แอปอยู่โหมดมืด แล้วกลับมาหน้าอื่นยัง**มืด**เหมือนเดิม
- refresh ในโหมดมืด → **ไม่มีแฟลชขาว**
- โหมดสว่าง: เทียบกับ `git stash` ของเดิม — **ต้องแยกไม่ออก**

- [ ] **Step 4: อัปเดตเอกสาร**

- `features.json` — เพิ่ม F46 ใน phase_5 (สร้าง phase ใหม่ `"id": "phase_5"`, `"name": "Phase 5 — Polish"`), `progressSummary` เป็น 54/54, `currentPhase: "phase_5"`
- `CLAUDE.md` — เพิ่ม Key Design Decision ข้อ 10: *"สีทั้งแอปมาจาก token ใน `index.css` เท่านั้น — ห้าม `bg-white`/`text-slate-*` ใน component ใหม่ (`verify-no-hardcoded-colors.ts` บังคับ). ธีมเป็นของเครื่อง ไม่ sync ขึ้น Drive"*
- `UXUI.md` — แทนที่ section 11 (dark surfaces ที่เขียนเผื่อไว้แต่ไม่เคยทำงาน) ด้วยตาราง token จริง

- [ ] **Step 5: commit ปิดงาน**

```bash
git add features.json CLAUDE.md UXUI.md
git commit -m "docs: F46 dark mode เสร็จ — token layer, ปุ่มสามจังหวะ, PDF ขาวเสมอ"
```

---

## สิ่งที่ไม่ทำในงานนี้ (จดไว้ กันเผลอ)

- **ไม่ปรับดีไซน์** — เจออะไรน่าปรับตอนไล่ดูโหมดมืด ให้จดไว้ทำใน "mobile UX polish" (งานที่ 2 ของ Phase 5)
- **ไม่ยุบ `CATEGORY_HEX_COLORS` ที่ซ้ำกัน 3 ที่** (ExpensePieChart / TrendAnalysis / expense-categories.ts) — เป็นหนี้ที่มีอยู่ก่อน ไม่ใช่หนี้ที่งานนี้ก่อ และมันไม่ขวางโหมดมืด
- **ไม่ทำ theme หลายสี** (sepia / high-contrast) — YAGNI
