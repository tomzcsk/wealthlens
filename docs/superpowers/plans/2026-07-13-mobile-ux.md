# F47 — Mobile UX Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** ใช้ WealthLens ด้วยนิ้วโป้งข้างเดียวบนมือถือได้จริง — กรอกรายจ่าย, เช็คยอด, ดูตาราง — โดยเดสก์ท็อปไม่ขยับแม้แต่พิกเซลเดียว

**Architecture:** ทะเบียนเมนูแหล่งเดียว (`src/lib/nav.ts`, pure) ป้อนทั้ง `Sidebar` (เดสก์ท็อป) และ `BottomNav` (มือถือ) ใหม่ — วันนี้ Sidebar hardcode เมนูไว้ในตัวเอง ถ้าเพิ่มแถบล่างโดยไม่แตะ จะได้เมนูสองชุดที่หลุดจากกัน. แถบล่าง + ปุ่มลอย "+" แทนแฮมเบอร์เกอร์มุมซ้ายบน. ตารางกว้างตรึงคอลัมน์แรกแทนการแตกเป็นการ์ด (กฎเดิม: ตัวเลขในตารางมีไว้เทียบกัน). ทุกอย่างพิสูจน์ด้วย `npm run verify:mobile` — Playwright เปิดแอปจริงที่ 390×844 แล้ววัด DOM

**Tech Stack:** React 18 + Vite + Tailwind (`md:` breakpoint = 768px) + react-router + Zustand. Motion ผ่าน `src/components/motion/` เท่านั้น (กฎ F42 — ห้าม import framer-motion ตรงใน page/feature). verify script รันด้วย `npx tsx --tsconfig tsconfig.app.json scripts/<file>.ts` **ใหม่:** `playwright` (devDependency)

**Spec:** `docs/superpowers/specs/2026-07-13-mobile-ux-design.md`

---

## กฎเหล็ก

1. **เดสก์ท็อปห้ามขยับ** — ทุก class ที่เพิ่มต้องมี breakpoint กำกับ (`md:hidden`, `min-h-11 md:min-h-0`) กฎ M6 ใน `verify-mobile` บังคับไว้
2. **ไม่แตกตารางเป็นการ์ด** — ทำลายการเทียบข้ามแถว ซึ่งเป็นเหตุผลเดียวที่ตารางมีอยู่
3. **ห้าม `overflow-x: hidden` ที่ `body`** เพื่อกลบ M1 — นั่นคือซ่อนบั๊ก ไม่ใช่แก้บั๊ก ต้องหาตัวที่ล้นแล้วแก้ที่ต้นเหตุ

---

## File Structure

**สร้างใหม่**
| ไฟล์ | หน้าที่ |
|---|---|
| `src/lib/nav.ts` | ทะเบียนเมนู pure — `NAV_ITEMS`, `mobilePrimaryItems()`, `mobileMoreItems()`, `desktopGroups()` |
| `src/components/layout/BottomNav.tsx` | แถบล่าง 5 แถบ + sheet "อื่นๆ" (`md:hidden`) |
| `src/components/layout/AddFab.tsx` | ปุ่มลอย "+" (`md:hidden`) → `/monthly?add=expense` |
| `scripts/verify-nav.ts` | ตรวจทะเบียนเมนู (node ล้วน) |
| `scripts/verify-mobile.ts` | Playwright: M1–M6 ที่ 390×844 + 1280px |

**แก้**
| ไฟล์ | แก้อะไร |
|---|---|
| `src/components/layout/Sidebar.tsx` | อ่านจากทะเบียน · **ลบแฮมเบอร์เกอร์ + drawer + framer-motion ออกทั้งหมด** (เมนูอยู่ล่างแล้ว) |
| `src/components/layout/Layout.tsx` | mount `<BottomNav />` + `<AddFab />` · เผื่อที่ว่างท้ายหน้าให้แถบล่าง |
| `src/components/layout/Header.tsx` | มือถือ: ชื่อหน้าเต็ม, ซ่อนปุ่ม "+ เพิ่มรายการ" |
| `src/components/forms/ExpenseList.tsx` | รับ prop `autoOpenAdd` |
| `src/pages/MonthlyPage.tsx` | อ่าน `?add=expense` · ซ่อมปุ่ม "← กลับภาพรวม" ที่ล้นขอบจอ |
| `src/pages/TaxCalculatorPage.tsx` | ซ่อม section ที่ล้น 361px |
| `src/components/dashboard/KpiCardGrid.tsx` | `grid-cols-2` บนมือถือ (วันนี้ `grid-cols-1`) |
| 5 ไฟล์ตาราง | ตรึงคอลัมน์แรก |
| `package.json` | devDep `playwright` + script `verify:mobile` |

---

## Task 1: ทะเบียนเมนู (`src/lib/nav.ts`)

**Files:**
- Create: `src/lib/nav.ts`
- Create: `scripts/verify-nav.ts`

- [ ] **Step 1: เขียน verify ที่ยังแดง — `scripts/verify-nav.ts`**

```ts
/**
 * Verification for F47 — ทะเบียนเมนู.
 *   npx tsx --tsconfig tsconfig.app.json scripts/verify-nav.ts
 *
 * ทะเบียนนี้ป้อนทั้ง Sidebar (เดสก์ท็อป) และ BottomNav (มือถือ) — ถ้ามันเพี้ยน
 * เมนูสองชุดจะหลุดจากกันโดยไม่มี error ให้เห็น
 */
import { readFileSync } from 'node:fs';

import {
  NAV_ITEMS,
  desktopGroups,
  mobileMoreItems,
  mobilePrimaryItems,
} from '../src/lib/nav';

let failures = 0;
const assert = (label: string, ok: boolean, detail = ''): void => {
  if (!ok) failures += 1;
  console.log(`${ok ? '✅' : '❌'} ${label}${ok || !detail ? '' : ` — ${detail}`}`);
};

console.log('\n— แถบล่างมี 5 ช่อง: 4 เมนู + "อื่นๆ" —');
const primary = mobilePrimaryItems();
assert(
  `mobilePrimary = 4 พอดี (ได้ ${primary.length})`,
  primary.length === 4,
  'มากกว่านี้แถบล่างจะล้น น้อยกว่านี้จะมีช่องว่าง',
);

console.log('\n— ไม่มีอะไรตกหล่นหรือซ้ำ —');
const more = mobileMoreItems();
assert(
  'primary + more = ทั้งทะเบียน',
  primary.length + more.length === NAV_ITEMS.length,
);
const paths = NAV_ITEMS.map((i) => i.path);
assert('ไม่มี path ซ้ำ', new Set(paths).size === paths.length);
const labels = NAV_ITEMS.map((i) => i.label);
assert('ไม่มี label ซ้ำ', new Set(labels).size === labels.length);

console.log('\n— เดสก์ท็อป: ทุกเมนูอยู่ในกลุ่มใดกลุ่มหนึ่ง —');
const grouped = desktopGroups().flat();
assert(
  `desktopGroups ครอบทุกเมนู (${grouped.length}/${NAV_ITEMS.length})`,
  grouped.length === NAV_ITEMS.length,
);

console.log('\n— ทุกเมนูชี้ไป route ที่มีจริงใน App.tsx —');
// อ่าน App.tsx ตรง ๆ: เมนูที่ชี้ไป route ที่ไม่มี = ลิงก์ตาย ไม่มี error ให้เห็น
const app = readFileSync('src/App.tsx', 'utf8');
const routes = new Set(
  [...app.matchAll(/path="([^"]+)"/g)].map(([, p]) => `/${p}`),
);
routes.add('/'); // <Route index>
for (const item of NAV_ITEMS) {
  assert(`${item.label} → ${item.path}`, routes.has(item.path));
}

console.log(failures === 0 ? '\n✅ ผ่านทั้งหมด' : `\n❌ ล้มเหลว ${failures} ข้อ`);
process.exit(failures === 0 ? 0 : 1);
```

- [ ] **Step 2: รันให้เห็นว่าแดง**

Run: `npx tsx --tsconfig tsconfig.app.json scripts/verify-nav.ts`
Expected: `Cannot find module '../src/lib/nav'`

- [ ] **Step 3: เขียน `src/lib/nav.ts`**

ค่าทั้งหมดยกมาจาก `Sidebar.tsx` ของเดิม (`NAV_GROUPS`) — **ห้ามเปลี่ยน label/icon/ลำดับ** เดสก์ท็อปต้องหน้าตาเหมือนเดิมเป๊ะ

```ts
/**
 * WealthLens — ทะเบียนเมนู (F47).
 *
 * แหล่งความจริงเดียวของ nav: Sidebar (เดสก์ท็อป) และ BottomNav (มือถือ)
 * อ่านจากที่นี่ทั้งคู่. ก่อนหน้านี้ Sidebar hardcode รายการไว้ในตัวเอง —
 * พอเพิ่มแถบล่าง มันจะกลายเป็นเมนูสองชุดที่หลุดจากกันวันใดวันหนึ่ง
 * (เพิ่มหน้าใหม่แล้วลืมอีกที่ ไม่มี error ให้เห็น)
 *
 * pure: ไม่ import React — ทดสอบใน node ได้ (หลักเดียวกับ lib/motion.ts, lib/theme.ts)
 */

export interface NavItem {
  path: string;
  label: string;
  icon: string;
  /** react-router `end` — ใช้กับ '/' เท่านั้น ไม่งั้นมัน active ทุกหน้า */
  end?: boolean;
  /**
   * URL อื่นที่ควรถือว่าเมนูนี้ active ด้วย.
   * หนี้สิน (/loans) เป็นเจ้าของทั้งแท็บ ผ่อนของ (/installments) และหนี้ระยะยาว
   */
  alsoActiveOn?: readonly string[];
  /** อยู่บนแถบล่าง (4 อัน) หรืออยู่ใน sheet "อื่นๆ" */
  mobilePrimary: boolean;
  /**
   * กลุ่มบน sidebar เดสก์ท็อป — คงลำดับเดิมไว้เป๊ะ:
   *   1 "วันนี้เป็นยังไง / จะบันทึกอะไร"
   *   2 "ฐานะฉันเป็นยังไง" (ความมั่งคั่งนำหน้า เพราะเป็นบทสรุปของอีกสามอัน)
   *   3 "เอาข้อมูลไปคิดต่อ"
   */
  group: 1 | 2 | 3;
}

export const NAV_ITEMS: readonly NavItem[] = [
  { path: '/', label: 'ภาพรวม', icon: '🏠', end: true, mobilePrimary: true, group: 1 },
  { path: '/monthly', label: 'รายเดือน', icon: '📊', mobilePrimary: true, group: 1 },
  { path: '/wealth', label: 'ความมั่งคั่ง', icon: '💎', mobilePrimary: false, group: 2 },
  { path: '/accounts', label: 'บัญชีธนาคาร', icon: '🏦', mobilePrimary: true, group: 2 },
  { path: '/gold', label: 'ทองคำ', icon: '🪙', mobilePrimary: false, group: 2 },
  {
    path: '/loans',
    label: 'หนี้สิน',
    icon: '💰',
    alsoActiveOn: ['/installments'],
    mobilePrimary: false,
    group: 2,
  },
  { path: '/analytics', label: 'วิเคราะห์', icon: '📈', mobilePrimary: true, group: 3 },
  { path: '/tax', label: 'ภาษี', icon: '🧮', mobilePrimary: false, group: 3 },
  { path: '/settings', label: 'ตั้งค่า', icon: '⚙️', mobilePrimary: false, group: 3 },
];

export const mobilePrimaryItems = (): NavItem[] =>
  NAV_ITEMS.filter((i) => i.mobilePrimary);

export const mobileMoreItems = (): NavItem[] =>
  NAV_ITEMS.filter((i) => !i.mobilePrimary);

export const desktopGroups = (): NavItem[][] =>
  ([1, 2, 3] as const).map((g) => NAV_ITEMS.filter((i) => i.group === g));

/** เมนูนี้ active อยู่ไหม เมื่อ url ปัจจุบันคือ pathname */
export const isNavActive = (item: NavItem, pathname: string): boolean => {
  if (item.end) return pathname === item.path;
  if (pathname === item.path) return true;
  if (pathname.startsWith(`${item.path}/`)) return true;
  return item.alsoActiveOn?.includes(pathname) ?? false;
};
```

- [ ] **Step 4: เพิ่มการตรวจ `isNavActive` ต่อท้าย `scripts/verify-nav.ts`** (ก่อนบรรทัดสรุป)

```ts
console.log('\n— isNavActive —');
const home = NAV_ITEMS.find((i) => i.path === '/')!;
const loans = NAV_ITEMS.find((i) => i.path === '/loans')!;
const monthly = NAV_ITEMS.find((i) => i.path === '/monthly')!;
assert("ภาพรวม active ที่ '/'", isNavActive(home, '/'));
assert("ภาพรวม ไม่ active ที่ '/monthly' (end)", !isNavActive(home, '/monthly'));
assert('รายเดือน active ที่ /monthly', isNavActive(monthly, '/monthly'));
assert('หนี้สิน active ที่ /installments ด้วย', isNavActive(loans, '/installments'));
assert('หนี้สิน ไม่ active ที่ /gold', !isNavActive(loans, '/gold'));
```
เพิ่ม `isNavActive` เข้า import ที่หัวไฟล์

- [ ] **Step 5: รันให้เขียว**

Run: `npx tsx --tsconfig tsconfig.app.json scripts/verify-nav.ts`
Expected: `✅ ผ่านทั้งหมด` exit 0
Run: `npm run typecheck && npm run lint`

- [ ] **Step 6: commit**

```bash
git add src/lib/nav.ts scripts/verify-nav.ts
git commit -m "feat(nav): ทะเบียนเมนูแหล่งเดียว (pure) + verify (F47)"
```

---

## Task 2: ประตู `verify:mobile` (Playwright) — เขียนก่อนแก้ ให้มันแดงตามความจริง

**Files:**
- Modify: `package.json`
- Create: `scripts/verify-mobile.ts`

- [ ] **Step 1: ติดตั้ง Playwright**

```bash
npm install -D playwright
npx playwright install chromium
```

- [ ] **Step 2: `package.json` — เพิ่ม script**

```json
    "verify:mobile": "vite build --mode verify && tsx --tsconfig tsconfig.app.json scripts/verify-mobile.ts",
```

- [ ] **Step 3: สร้าง `.env.verify`** (ที่ราก repo)

```
VITE_GOOGLE_CLIENT_ID=
```

> **ทำไม:** แอปมี login gate — `requireSignIn = isReady && !isSignedIn` และ `isReady` มาจาก `isAuthConfigured()` ซึ่งเป็นจริงเมื่อมี `VITE_GOOGLE_CLIENT_ID`. build โหมด `verify` โดยไม่ใส่ client id → `!isReady` → แอปตกเข้าโหมด local-only แล้ว dashboard ขึ้นเลย ไม่ต้องปลอม token ไม่ต้องเจาะ auth

- [ ] **Step 4: เขียน `scripts/verify-mobile.ts`**

```ts
/**
 * Verification for F47 — mobile UX.
 *   npm run verify:mobile
 *
 * dark mode พิสูจน์ได้ด้วยการอ่านค่าสี แต่ mobile พิสูจน์ได้จาก "จอที่ render
 * แล้ว" เท่านั้น — element ล้นขอบ, ปุ่มเล็กกว่านิ้ว, แถบล่างครบไหม วัดจาก DOM จริง
 *
 * เปิด dist/ ที่ 390×844 (iPhone) แล้ววิ่งทุกหน้า + เช็คเดสก์ท็อป 1280px ปิดท้าย
 */
import { createServer } from 'node:http';
import { readFileSync, existsSync } from 'node:fs';
import { extname, join } from 'node:path';

import { chromium, type Page } from 'playwright';

import seedData from '../src/data/seedData';

const PORT = 4178;
const ROUTES = [
  '/',
  '/monthly',
  '/analytics',
  '/accounts',
  '/wealth',
  '/loans',
  '/gold',
  '/tax',
  '/settings',
];

let failures = 0;
const assert = (label: string, ok: boolean, detail = ''): void => {
  if (!ok) failures += 1;
  console.log(`${ok ? '✅' : '❌'} ${label}${ok || !detail ? '' : ` — ${detail}`}`);
};

// ---------- static server สำหรับ dist/ (SPA fallback) ----------
const MIME: Record<string, string> = {
  '.html': 'text/html',
  '.js': 'text/javascript',
  '.css': 'text/css',
  '.svg': 'image/svg+xml',
  '.png': 'image/png',
  '.json': 'application/json',
};

const server = createServer((req, res) => {
  const url = (req.url ?? '/').split('?')[0];
  let file = join('dist', url === '/' ? 'index.html' : url);
  if (!existsSync(file) || !extname(file)) file = join('dist', 'index.html');
  res.setHeader('content-type', MIME[extname(file)] ?? 'text/plain');
  res.end(readFileSync(file));
});
await new Promise<void>((r) => server.listen(PORT, r));

const browser = await chromium.launch();

/** เปิดหน้าโดยมีข้อมูลจริงของ Tom อยู่ใน LocalStorage แล้ว */
const openApp = async (width: number, height: number): Promise<Page> => {
  const ctx = await browser.newContext({ viewport: { width, height } });
  // store เริ่มต้นเป็น "หน้าว่าง" (buildInitialState → emptyData) — ถ้าไม่ seed
  // เราจะไปทดสอบตารางเปล่า ซึ่งไม่มีคอลัมน์ให้ล้นตั้งแต่แรก = ทดสอบหลอกตัวเอง
  await ctx.addInitScript(
    ([key, data]) => {
      localStorage.setItem(
        key,
        JSON.stringify({
          state: {
            data,
            selectedYear: 2025,
            selectedMonth: 7,
            lastUpdated: new Date(0).toISOString(),
          },
          version: 1,
        }),
      );
    },
    ['wealthlens_data', seedData] as const,
  );
  return ctx.newPage();
};

// ══════════ มือถือ 390×844 ══════════
const page = await openApp(390, 844);

for (const route of ROUTES) {
  await page.goto(`http://localhost:${PORT}${route}`, { waitUntil: 'networkidle' });
  await page.waitForTimeout(400); // ให้ chunk + motion ลงตัว

  console.log(`\n── ${route}`);

  // ---------- M1: ไม่มีอะไรล้นขอบจอ ----------
  const bleed = await page.evaluate(() => {
    const vw = document.documentElement.clientWidth;
    const over = [...document.querySelectorAll('*')]
      .filter((e) => e.getBoundingClientRect().right > vw + 1)
      .map((e) => `${e.tagName.toLowerCase()}.${(e.className || '').toString().split(' ')[0]}`);
    return {
      scrollW: document.documentElement.scrollWidth,
      vw,
      culprits: [...new Set(over)].slice(0, 5),
    };
  });
  assert(
    `M1 ไม่ล้นขอบจอ (scrollWidth ${bleed.scrollW} ≤ ${bleed.vw})`,
    bleed.scrollW <= bleed.vw,
    bleed.culprits.join(', '),
  );

  // ---------- M2: ปุ่มกดโดน ≥ 44px ----------
  const small = await page.evaluate(() => {
    const sel = 'button, a, select, input[type=checkbox]';
    return [...document.querySelectorAll(sel)]
      .filter((e) => {
        const r = e.getBoundingClientRect();
        if (r.width === 0 || r.height === 0) return false; // ซ่อนอยู่
        if (e.closest('[data-tap-exempt]')) return false; // ยกเว้นโดยตั้งใจ
        return r.height < 44;
      })
      .map((e) => `${e.tagName.toLowerCase()}[${(e.textContent || '').trim().slice(0, 12)}] ${Math.round(e.getBoundingClientRect().height)}px`);
  });
  assert(
    `M2 ปุ่มทุกตัว ≥ 44px (เล็กเกิน ${small.length} ตัว)`,
    small.length === 0,
    small.slice(0, 6).join(' · '),
  );

  // ---------- M3: แถบล่างครบและบอกตำแหน่งปัจจุบัน ----------
  const nav = await page.evaluate(() => {
    const bar = document.querySelector('[data-testid="bottom-nav"]');
    if (!bar) return null;
    const tabs = [...bar.querySelectorAll('a, button')];
    return {
      tabs: tabs.length,
      current: tabs.filter((t) => t.getAttribute('aria-current') === 'page').length,
    };
  });
  assert(`M3 แถบล่างมี 5 ช่อง`, nav?.tabs === 5, nav ? `ได้ ${nav.tabs}` : 'ไม่เจอแถบล่าง');
  // '/gold' ฯลฯ อยู่ใน sheet "อื่นๆ" — ไม่มีแถบไหน aria-current ก็ถูกแล้ว
  const expectCurrent = ['/', '/monthly', '/accounts', '/analytics'].includes(route);
  assert(
    `M3 แถบปัจจุบันติดธง aria-current (คาด ${expectCurrent ? 1 : 0})`,
    nav?.current === (expectCurrent ? 1 : 0),
  );

  // ---------- M4: ตารางกว้างเกินจอ → ต้องตรึงคอลัมน์แรก ----------
  const tables = await page.evaluate(() => {
    return [...document.querySelectorAll('table')].map((t) => {
      const wide = t.scrollWidth > document.documentElement.clientWidth;
      const first = t.querySelector('tbody tr > *:first-child');
      const sticky = first ? getComputedStyle(first).position === 'sticky' : false;
      return { wide, sticky, rows: t.querySelectorAll('tbody tr').length };
    });
  });
  for (const [i, t] of tables.entries()) {
    if (!t.wide || t.rows === 0) continue;
    assert(`M4 ตาราง #${i + 1} (กว้างเกินจอ) ตรึงคอลัมน์แรก`, t.sticky);
  }

  // ---------- M5: ปุ่มลอยไม่ทับ control อื่น ----------
  const fabClash = await page.evaluate(() => {
    const fab = document.querySelector('[data-testid="add-fab"]');
    if (!fab) return 'ไม่เจอปุ่มลอย';
    const r = fab.getBoundingClientRect();
    const under = document.elementsFromPoint(r.left + r.width / 2, r.top + r.height / 2);
    // ตัวแรกต้องเป็น FAB เอง (หรือลูกของมัน) — ถ้ามี control อื่นทับอยู่ = ชน
    const top = under[0];
    return fab.contains(top) ? null : `ทับกับ ${top?.tagName}`;
  });
  assert('M5 ปุ่มลอยกดโดน ไม่มีอะไรทับ', fabClash === null, String(fabClash));
}

// ══════════ เดสก์ท็อป 1280 — ต้องไม่มีอะไรของมือถือโผล่ ══════════
console.log('\n── เดสก์ท็อป 1280×800');
const desk = await openApp(1280, 800);
await desk.goto(`http://localhost:${PORT}/`, { waitUntil: 'networkidle' });
await desk.waitForTimeout(400);
const deskState = await desk.evaluate(() => ({
  bottomNav: !!document.querySelector('[data-testid="bottom-nav"]')?.checkVisibility(),
  fab: !!document.querySelector('[data-testid="add-fab"]')?.checkVisibility(),
  sidebar: !!document.querySelector('aside')?.checkVisibility(),
  hamburger: !!document.querySelector('[aria-label="เปิดเมนู"]'),
}));
assert('M6 เดสก์ท็อป: ไม่มีแถบล่าง', !deskState.bottomNav);
assert('M6 เดสก์ท็อป: ไม่มีปุ่มลอย', !deskState.fab);
assert('M6 เดสก์ท็อป: sidebar อยู่ครบ', deskState.sidebar);
assert('M6 แฮมเบอร์เกอร์ถูกลบทิ้งแล้ว (เมนูอยู่แถบล่าง)', !deskState.hamburger);

await browser.close();
server.close();

console.log(failures === 0 ? '\n✅ ผ่านทั้งหมด' : `\n❌ ล้มเหลว ${failures} ข้อ`);
process.exit(failures === 0 ? 0 : 1);
```

- [ ] **Step 5: รันให้เห็นว่าแดง — และ *บันทึกว่าแดงตรงไหน***

Run: `npm run verify:mobile`
Expected (สภาพวันนี้ — นี่คือ baseline ที่ต้องทำให้เขียว):
- **M1 แดงที่ `/monthly`** (scrollWidth 411 > 386) และอาจแดงที่ `/tax`
- **M2 แดงเกือบทุกหน้า** (รายเดือน 32 ตัว, ทอง 24, บัญชี 23)
- **M3 แดงทุกหน้า** (ยังไม่มีแถบล่าง)
- **M4 แดง** ที่ `/` และ `/analytics` (ตารางกว้างแต่ไม่ตรึง)
- **M5 แดงทุกหน้า** (ยังไม่มีปุ่มลอย)
- **M6 แฮมเบอร์เกอร์ยังอยู่** → แดง 1 ข้อ

จดจำนวนที่แดงไว้ในข้อความ commit — มันคือ "ก่อน" ที่จะเอาไปเทียบตอนจบ

- [ ] **Step 6: commit** (ประตูที่แดงอยู่ ไม่ใช่ความล้มเหลว — มันคือความจริงที่วัดได้)

```bash
git add package.json package-lock.json .env.verify scripts/verify-mobile.ts
git commit -m "test(mobile): ประตู verify:mobile — Playwright วัด M1-M6 ที่ 390px (F47)"
```

---

## Task 3: Sidebar อ่านจากทะเบียน + ลบ drawer/แฮมเบอร์เกอร์

**Files:**
- Modify: `src/components/layout/Sidebar.tsx`

- [ ] **Step 1: เขียน `Sidebar.tsx` ใหม่ทั้งไฟล์**

drawer + แฮมเบอร์เกอร์ + framer-motion หายไปทั้งหมด (เมนูย้ายลงแถบล่างใน Task 4) เหลือแค่ rail เดสก์ท็อป — ไฟล์สั้นลงจาก ~190 บรรทัดเหลือ ~60

```tsx
/**
 * WealthLens — Sidebar (เดสก์ท็อปเท่านั้น).
 *
 * 240px rail ซ้ายมือ (UXUI.md §5.1). บนมือถือไม่มี sidebar แล้ว —
 * เมนูอยู่ที่ BottomNav (F47). แฮมเบอร์เกอร์มุมซ้ายบนถูกลบทิ้ง: มันอยู่ในจุดที่
 * นิ้วโป้งเอื้อมยากที่สุดบนจอ 6 นิ้ว และมันกิน 40px จาก header จนชื่อหน้าโดนตัด
 *
 * รายการเมนูมาจาก src/lib/nav.ts — ทะเบียนเดียวกับที่ BottomNav ใช้
 */
import type { ReactNode } from 'react';
import { NavLink, useLocation } from 'react-router-dom';

import { desktopGroups, isNavActive } from '@/lib/nav';

import BuildInfo from './BuildInfo';

const linkBase =
  'flex items-center gap-3 px-4 py-2.5 rounded-lg text-sm transition-colors';
const linkInactive = 'text-ink-500 hover:bg-raised hover:text-ink-900';
const linkActive = 'bg-primary-50 text-primary-ink font-semibold';

export const Sidebar = (): ReactNode => {
  const { pathname } = useLocation();

  return (
    <aside
      className="hidden md:flex md:flex-col w-[240px] h-screen sticky top-0 bg-card border-r border-ink-200"
      aria-label="เมนู"
    >
      <div className="px-4 pt-6 pb-8">
        <div className="text-2xl font-bold text-primary-ink leading-none">
          WealthLens
        </div>
        <div className="mt-1 text-xs text-ink-500">บัญชีส่วนตัว</div>
      </div>

      <nav className="px-2" aria-label="เมนูหลัก">
        {desktopGroups().map((group, groupIndex) => (
          <div
            key={group[0].path}
            className={
              groupIndex > 0
                ? 'mt-2 flex flex-col gap-1 border-t border-ink-200 pt-2'
                : 'flex flex-col gap-1'
            }
          >
            {group.map((item) => (
              <NavLink
                key={item.path}
                to={item.path}
                end={item.end}
                className={`${linkBase} ${
                  isNavActive(item, pathname) ? linkActive : linkInactive
                }`}
              >
                <span aria-hidden="true" className="text-base leading-none">
                  {item.icon}
                </span>
                <span>{item.label}</span>
              </NavLink>
            ))}
          </div>
        ))}
      </nav>

      <BuildInfo />
    </aside>
  );
};

export default Sidebar;
```

- [ ] **Step 2: ตรวจว่าเดสก์ท็อปไม่ขยับ**

Run: `npm run typecheck && npm run lint && npm run build`
Run: `npm run dev` แล้วเปิดที่ความกว้างเดสก์ท็อป — เมนู 9 รายการ 3 กลุ่ม ลำดับเดิม, highlight ถูกต้อง, `/installments` ยัง highlight ที่ "หนี้สิน"

- [ ] **Step 3: commit**

```bash
git add src/components/layout/Sidebar.tsx
git commit -m "refactor(nav): Sidebar อ่านจากทะเบียน + ลบ drawer/แฮมเบอร์เกอร์ (F47)"
```

---

## Task 4: `BottomNav` + sheet "อื่นๆ"

**Files:**
- Create: `src/components/layout/BottomNav.tsx`
- Modify: `src/components/layout/Layout.tsx`

- [ ] **Step 1: `src/components/layout/BottomNav.tsx`**

**กฎ F42:** ห้าม import `framer-motion` ตรง — sheet ใช้ `<Modal>` ที่มีอยู่แล้ว (มัน animate ผ่าน motion layer ให้เรียบร้อยแล้ว) แทนการเขียน sheet ใหม่

```tsx
/**
 * WealthLens — แถบล่าง (มือถือเท่านั้น, F47).
 *
 * 4 เมนูหลัก + "อื่นๆ" ที่เปิด sheet รวมอีก 5 เมนู. อยู่ล่างจอเพราะนั่นคือที่ที่
 * นิ้วโป้งไปถึง — ของเดิมเป็นแฮมเบอร์เกอร์มุมซ้ายบน ซึ่งเอื้อมยากที่สุด
 *
 * safe-area-inset-bottom: ไม่ใส่แล้วแถบจะไปนอนใต้แถบ home ของ iPhone
 */
import { useState, type ReactNode } from 'react';
import { NavLink, useLocation, useNavigate } from 'react-router-dom';

import Modal from '@/components/ui/Modal';
import { isNavActive, mobileMoreItems, mobilePrimaryItems } from '@/lib/nav';

const tabBase =
  'flex flex-col items-center justify-center gap-0.5 min-h-[56px] text-[10px] font-medium transition-colors';
const tabOn = 'text-primary-ink';
const tabOff = 'text-ink-500';

export const BottomNav = (): ReactNode => {
  const { pathname } = useLocation();
  const navigate = useNavigate();
  const [moreOpen, setMoreOpen] = useState(false);

  const more = mobileMoreItems();
  const moreActive = more.some((item) => isNavActive(item, pathname));

  const go = (path: string): void => {
    setMoreOpen(false);
    navigate(path);
  };

  return (
    <>
      <nav
        data-testid="bottom-nav"
        aria-label="เมนูหลัก"
        className="md:hidden fixed bottom-0 inset-x-0 z-30 grid grid-cols-5 bg-card border-t border-ink-200 pb-[env(safe-area-inset-bottom)]"
      >
        {mobilePrimaryItems().map((item) => {
          const active = isNavActive(item, pathname);
          return (
            <NavLink
              key={item.path}
              to={item.path}
              end={item.end}
              aria-current={active ? 'page' : undefined}
              className={`${tabBase} ${active ? tabOn : tabOff}`}
            >
              <span aria-hidden="true" className="text-xl leading-none">
                {item.icon}
              </span>
              <span>{item.label}</span>
            </NavLink>
          );
        })}

        <button
          type="button"
          onClick={() => setMoreOpen(true)}
          aria-current={moreActive ? 'page' : undefined}
          aria-haspopup="dialog"
          className={`${tabBase} ${moreActive ? tabOn : tabOff}`}
        >
          <span aria-hidden="true" className="text-xl leading-none">
            ⋯
          </span>
          <span>อื่นๆ</span>
        </button>
      </nav>

      <Modal open={moreOpen} onClose={() => setMoreOpen(false)} title="เมนูอื่นๆ" size="sm">
        <div className="flex flex-col gap-1 pb-2">
          {more.map((item) => (
            <button
              key={item.path}
              type="button"
              onClick={() => go(item.path)}
              className={`flex items-center gap-3 min-h-[52px] px-3 rounded-xl text-sm font-medium hover:bg-hover ${
                isNavActive(item, pathname) ? 'text-primary-ink' : 'text-ink-900'
              }`}
            >
              <span aria-hidden="true" className="text-lg w-6 text-center">
                {item.icon}
              </span>
              <span className="flex-1 text-left">{item.label}</span>
              <span aria-hidden="true" className="text-ink-400">
                ›
              </span>
            </button>
          ))}
        </div>
      </Modal>
    </>
  );
};

export default BottomNav;
```

- [ ] **Step 2: `Layout.tsx` — mount แถบล่าง + เผื่อที่ว่างท้ายหน้า**

ใน `Layout.tsx` เพิ่ม import `BottomNav` แล้วแก้ `<main>` กับต่อท้าย `<Sidebar />`:

```jsx
        <div className="min-h-screen bg-surface md:grid md:grid-cols-[240px_1fr]">
          <Sidebar />
          <div className="flex flex-col min-w-0">
            <Header />
            {/* pb-24 เผื่อที่ให้แถบล่าง ไม่งั้นเนื้อหาท้ายหน้าถูกแถบทับ */}
            <main className="flex-1 p-4 pb-24 md:p-8 md:pb-8">
              <PageTransition />
            </main>
          </div>
          <BottomNav />
          <SessionWatcher />
        </div>
```

(เดิม `<main>` เป็น `p-6 md:p-8` — บนมือถือ `p-4` ให้พื้นที่คืน 16px ต่อข้าง)

- [ ] **Step 3: ตรวจ**

Run: `npm run typecheck && npm run lint && npm run build`
Run: `npm run verify:mobile` → **M3 ต้องเขียวทุกหน้า** (M1/M2/M4/M5 ยังแดงอยู่ — ปกติ)

- [ ] **Step 4: commit**

```bash
git add src/components/layout/BottomNav.tsx src/components/layout/Layout.tsx
git commit -m "feat(nav): แถบล่าง 5 เมนู + sheet อื่นๆ (F47)"
```

---

## Task 5: ปุ่มลอย "+" → เปิดฟอร์มรายจ่าย

**Files:**
- Create: `src/components/layout/AddFab.tsx`
- Modify: `src/components/layout/Layout.tsx`
- Modify: `src/components/forms/ExpenseList.tsx`
- Modify: `src/pages/MonthlyPage.tsx`

- [ ] **Step 1: `src/components/layout/AddFab.tsx`**

```tsx
/**
 * WealthLens — ปุ่มลอยเพิ่มรายจ่าย (มือถือเท่านั้น, F47).
 *
 * ก่อนหน้านี้ปุ่ม "+ เพิ่มรายการ" ใน Header เป็น `hidden sm:inline-flex` —
 * ปุ่มที่ควรกดบ่อยที่สุด ไม่มีบนเครื่องที่ควรกดบ่อยที่สุด
 *
 * พาไป /monthly?add=expense → MonthlyPage สั่ง ExpenseList เปิดฟอร์มให้เลย
 * (รายจ่ายคืองานที่ทำบ่อยสุดบนมือถือ — ไม่ต้องถามก่อนว่าจะเพิ่มอะไร)
 */
import type { ReactNode } from 'react';
import { useNavigate } from 'react-router-dom';

export const AddFab = (): ReactNode => {
  const navigate = useNavigate();

  return (
    <button
      type="button"
      data-testid="add-fab"
      onClick={() => navigate('/monthly?add=expense')}
      aria-label="เพิ่มรายจ่าย"
      className="md:hidden fixed right-4 bottom-[calc(72px+env(safe-area-inset-bottom))] z-40 w-14 h-14 rounded-2xl bg-primary text-white text-2xl shadow-lg flex items-center justify-center transition motion-safe:active:scale-95"
    >
      <span aria-hidden="true">+</span>
    </button>
  );
};

export default AddFab;
```

- [ ] **Step 2: `Layout.tsx` — mount ต่อจาก `<BottomNav />`**

```jsx
          <BottomNav />
          <AddFab />
```
(เพิ่ม import ด้วย)

- [ ] **Step 3: `ExpenseList.tsx` — รับ prop `autoOpenAdd`**

เพิ่มใน `ExpenseListProps`:
```ts
  /** เปิดฟอร์ม "เพิ่มค่าใช้จ่าย" ทันทีที่ mount (มาจากปุ่มลอยบนมือถือ, F47). */
  autoOpenAdd?: boolean;
```

หา state ที่คุมฟอร์มเพิ่ม (ตัวที่ปุ่ม "+ เพิ่มค่าใช้จ่าย" เรียก) แล้วตั้งค่าเริ่มต้นจาก prop:
```ts
const [adding, setAdding] = useState(autoOpenAdd ?? false);
```
> ถ้าชื่อ state ในไฟล์ไม่ใช่ `adding` ให้ใช้ชื่อจริงในไฟล์ — **อย่าเปลี่ยนชื่อ state เดิม** งานนี้แค่ตั้งค่าเริ่มต้นให้มัน

- [ ] **Step 4: `MonthlyPage.tsx` — อ่าน query param แล้วส่งต่อ**

```tsx
import { useSearchParams } from 'react-router-dom';
...
  const [searchParams, setSearchParams] = useSearchParams();
  const autoOpenAdd = searchParams.get('add') === 'expense';

  // ล้าง param ทิ้งหลังใช้ครั้งเดียว — ไม่งั้น refresh/กดย้อนกลับแล้วฟอร์มเด้งซ้ำ
  useEffect(() => {
    if (autoOpenAdd) setSearchParams({}, { replace: true });
  }, [autoOpenAdd, setSearchParams]);
```
แล้วส่ง `autoOpenAdd={autoOpenAdd}` เข้า `<ExpenseList ... />`

> **ระวัง:** `autoOpenAdd` ต้องถูกอ่าน **ก่อน** effect ล้าง param ทำงาน — `useState(autoOpenAdd)` ใน ExpenseList อ่านตอน mount ซึ่งเกิดก่อน effect ของ parent จึงปลอดภัย. ถ้าย้ายไปเป็น `useEffect` ใน ExpenseList จะกลายเป็น race

- [ ] **Step 5: ตรวจของจริง**

Run: `npm run dev` → เปิดที่ 390px → กดปุ่มลอย → ต้องไป `/monthly` แล้ว **ฟอร์มรายจ่ายเปิดขึ้นเอง** และ url กลับเป็น `/monthly` (ไม่มี `?add=expense` ค้าง)
Run: `npm run verify:mobile` → **M5 เขียว**

- [ ] **Step 6: commit**

```bash
git add src/components/layout/AddFab.tsx src/components/layout/Layout.tsx src/components/forms/ExpenseList.tsx src/pages/MonthlyPage.tsx
git commit -m "feat(mobile): ปุ่มลอยเพิ่มรายจ่าย → เปิดฟอร์มให้เลย (F47)"
```

---

## Task 6: Header บนมือถือ

**Files:**
- Modify: `src/components/layout/Header.tsx`

- [ ] **Step 1: แก้ 3 จุด**

1. **ลบ spacer ของแฮมเบอร์เกอร์** — บรรทัด `<div className="md:hidden w-10" aria-hidden="true" />` (แฮมเบอร์เกอร์ไม่มีแล้ว มันเหลือแต่ที่ว่าง)
2. **ปุ่ม "+ เพิ่มรายการ"** เปลี่ยน `hidden sm:inline-flex` → `hidden md:inline-flex` (ปุ่มลอยทำหน้าที่นี้บนมือถือ; `sm` = 640px ซึ่งยังเป็นมือถือแนวนอน)
3. **padding** `px-6 md:px-8` → `px-4 md:px-8` (คืนพื้นที่ให้ชื่อหน้า)

- [ ] **Step 2: ตรวจ**

Run: `npm run dev` → ที่ 390px ชื่อหน้า "รายละเอียดรายเดือน" ต้องแสดงเต็ม ไม่โดนตัดเป็น "ภา…"
Run: `npm run typecheck && npm run lint`

- [ ] **Step 3: commit**

```bash
git add src/components/layout/Header.tsx
git commit -m "feat(mobile): header บางลง ชื่อหน้าเต็ม (F47)"
```

---

## Task 7: ซ่อมของที่ล้นขอบจอ (M1)

**Files:**
- Modify: `src/pages/MonthlyPage.tsx`
- Modify: `src/pages/TaxCalculatorPage.tsx`

- [ ] **Step 1: `MonthlyPage.tsx` — ปุ่ม "← กลับภาพรวม"**

ตัวการทำให้ทั้งหน้าเลื่อนแนวนอน: มันถูกบีบเหลือกว้าง 25px แต่ข้อความล้นไปถึง x=412 บนจอ 386px

แถวหัวเรื่อง (บรรทัด ~84–92) เป็น flex แถวเดียวที่ยัดทั้ง prev/next เดือน + ชื่อเดือน + ปุ่มกลับ → บนมือถือให้ห่อบรรทัด และปุ่มกลับไม่หด:

```jsx
        <button
          type="button"
          onClick={() => navigate('/')}
          className="shrink-0 whitespace-nowrap text-sm text-ink-500 hover:text-ink-900 min-h-11 md:min-h-0"
        >
          ← กลับภาพรวม
        </button>
```
และที่ container ของแถวนั้นเพิ่ม `flex-wrap gap-2`

- [ ] **Step 2: `TaxCalculatorPage.tsx` — section ที่ล้น**

หา element ที่ `getBoundingClientRect().right > 386` ที่ 390px (verify-mobile รายงานชื่อ class ให้) แล้วแก้ที่ต้นเหตุ — น่าจะเป็น `p-6` + ตารางใน section ที่ไม่มี `overflow-x-auto` ถ้าใช่ ใส่ `overflow-x-auto` ที่ตัวห่อตาราง และลด padding เป็น `p-4 md:p-6`

**ห้ามใส่ `overflow-x: hidden` ที่ body เพื่อกลบ** — นั่นคือซ่อนบั๊ก

- [ ] **Step 3: ตรวจ**

Run: `npm run verify:mobile` → **M1 เขียวทุกหน้า**

- [ ] **Step 4: commit**

```bash
git add src/pages/MonthlyPage.tsx src/pages/TaxCalculatorPage.tsx
git commit -m "fix(mobile): ซ่อมสองจุดที่ทำให้ทั้งหน้าเลื่อนแนวนอน (F47)"
```

---

## Task 8: ปุ่มกดโดน 44px (M2)

**Files:** ไฟล์ที่ `verify-mobile` รายงานว่าแดง (คาดว่า `ExpenseList`, `SavingsList`, `MonthTransactionList`, `BankAccountCard`, `GoldPage`, `MonthlySummaryTable`, `AnomalyAlerts`, `SubscriptionManager`)

- [ ] **Step 1: ให้ verify-mobile บอกว่าใครแดงบ้าง**

Run: `npm run verify:mobile 2>&1 | grep "M2"` — มันพิมพ์ tag + ข้อความ + ความสูงจริงของทุกปุ่มที่เล็กเกิน

- [ ] **Step 2: แก้ทีละไฟล์ ด้วยกฎเดียว**

ปุ่ม/ลิงก์ที่กดจริง → เพิ่ม `min-h-11 md:min-h-0` (44px บนมือถือ, เดสก์ท็อปเท่าเดิม)
ปุ่มไอคอนเล็ก (✏️ ✕ 🗑) → `min-h-11 min-w-11 md:min-h-0 md:min-w-0` พร้อม `flex items-center justify-center`

**ห้ามยัด `min-h-11` ใส่ทุก element แบบเหวี่ยงแห** — ตารางจะสูงจนอ่านยาก ทำเฉพาะ control ที่กดจริง

- [ ] **Step 3: ถ้ามีปุ่มที่ 44px แล้วพัง layout จริง ๆ**

ใส่ `data-tap-exempt` พร้อม **คอมเมนต์บอกเหตุผล** (verify-mobile ข้ามให้) — แต่ต้องเป็นข้อยกเว้นที่อธิบายได้ ไม่ใช่ทางลัดให้เขียว ถ้ามีเกิน 3 จุด แปลว่ากำลังโกงตัวเอง กลับไปคิดใหม่

- [ ] **Step 4: ตรวจ**

Run: `npm run verify:mobile` → **M2 เขียวทุกหน้า**
Run: `npm run dev` ที่เดสก์ท็อป → ตารางความสูงเท่าเดิม ไม่อ้วนขึ้น

- [ ] **Step 5: commit**

```bash
git commit -am "feat(mobile): พื้นที่กดขั้นต่ำ 44px เฉพาะบนมือถือ (F47)"
```

---

## Task 9: ความหนาแน่น — KPI 2 คอลัมน์ + กราฟเตี้ยลง

**Files:**
- Modify: `src/components/dashboard/KpiCardGrid.tsx`
- Modify: ไฟล์กราฟที่มีตัวห่อความสูงคงที่

- [ ] **Step 1: KPI 2×2 บนมือถือ**

`KpiCardGrid.tsx:81` วันนี้คือ:
```
grid grid-cols-1 gap-4 sm:grid-cols-2 md:grid-cols-2 lg:grid-cols-4
```
เปลี่ยนเป็น:
```
grid grid-cols-2 gap-3 sm:gap-4 md:grid-cols-2 lg:grid-cols-4
```
(ที่ 390px ได้ 2×2 แทน 4 ใบซ้อน — วันนี้ต้องเลื่อน 2 จอกว่าจะพ้น KPI)

ถ้าตัวเลขยาว (`฿2,598,100`) ล้นในการ์ดแคบ → ลดขนาดตัวเลขเฉพาะมือถือที่ `KpiCard.tsx`: `text-number-lg md:text-number-xl` **ห้ามใส่ `truncate`** — ตัวเลขเงินที่ถูกตัดคือตัวเลขที่โกหก

- [ ] **Step 2: กราฟเตี้ยลงบนมือถือ**

หา wrapper ของ `<ResponsiveContainer>` ในแต่ละกราฟ (มันต้องมีความสูงคงที่ ไม่งั้น ResponsiveContainer สูง 0) แล้วเปลี่ยนเป็น `h-64 md:h-80` (หรือค่าที่เตี้ยลงจากเดิม ~20% บนมือถือ)
ไฟล์: `IncomeExpenseChart.tsx`, `ExpensePieChart.tsx`, `TrendAnalysis.tsx` (2 กราฟ), `MultiYearComparison.tsx`

- [ ] **Step 3: ตรวจ**

Run: `npm run verify:mobile` → M1 ยังเขียว (การ์ด 2 คอลัมน์ต้องไม่ล้น)
วัดความยาวหน้า: ที่ 390px หน้า `/` ต้องสั้นลงจาก 3,540px อย่างน้อย ~25%

- [ ] **Step 4: commit**

```bash
git commit -am "feat(mobile): KPI 2 คอลัมน์ + กราฟเตี้ยลง — หน้าสั้นลง (F47)"
```

---

## Task 10: ตรึงคอลัมน์แรกในตารางกว้าง (M4)

**Files:**
- Modify: `src/components/dashboard/MonthlySummaryTable.tsx`
- Modify: `src/components/analytics/AllYearsSummary.tsx`
- Modify: `src/components/analytics/MultiYearComparison.tsx`
- Modify: `src/components/loans/LoanScheduleTable.tsx`
- Modify: `src/components/loans/PaymentLogTable.tsx`

- [ ] **Step 1: กฎเดียวใช้ทุกตาราง**

เซลล์แรกของทุกแถว (ทั้ง `<th>` ในหัวและ `<td>` ในตัว):
```
sticky left-0 z-10 bg-card
```
หัวตาราง (`thead th:first-child`) ต้อง `z-20` (มันต้องทับทั้งแถวและคอลัมน์)

> **สำคัญ:** พื้นต้องทึบ (`bg-card`) ไม่งั้นเนื้อหาที่เลื่อนผ่านจะทะลุมาเห็นข้างหลัง
> ถ้าแถวมีสีสลับ/hover ต้องให้เซลล์ตรึงใช้สีเดียวกับแถว ไม่งั้นคอลัมน์แรกจะสีไม่ตรงกับแถวที่เหลือ — ใช้ `bg-inherit` บนเซลล์ตรึง แล้วตั้งสีที่ `<tr>` แทน

เพิ่มเงาขอบขวาเพื่อบอกว่ามันตรึงอยู่:
```
after:absolute after:inset-y-0 after:right-0 after:w-2 after:translate-x-full
after:bg-gradient-to-r after:from-black/5 after:to-transparent
```
(หรือ `shadow-[8px_0_8px_-8px_rgba(2,6,23,0.22)]` ถ้าง่ายกว่า)

- [ ] **Step 2: ป้ายบอกว่ายังมีคอลัมน์ต่อ (มือถือเท่านั้น)**

ที่หัวการ์ดของตารางที่กว้างเกินจอ:
```jsx
<span className="md:hidden text-[10px] font-normal text-ink-400">
  เลื่อนดูคอลัมน์อื่น →
</span>
```

- [ ] **Step 3: ตรวจ**

Run: `npm run verify:mobile` → **M4 เขียว**
Run: `npm run dev` ที่ 390px → เลื่อนตารางแนวนอน คอลัมน์เดือน/ปีต้องอยู่กับที่ พื้นทึบ ไม่มีตัวเลขทะลุ
ที่เดสก์ท็อป → ตารางไม่กว้างเกินจอ เซลล์ sticky ไม่ทำอะไรเลย (ไม่มีผลข้างเคียง)

- [ ] **Step 4: commit**

```bash
git commit -am "feat(mobile): ตรึงคอลัมน์แรกในตารางกว้าง (F47)"
```

---

## Task 11: ปิดงาน — verify ทั้งชุด + เอกสาร

- [ ] **Step 1: verify ทุกตัวต้องเขียว**

```bash
for f in scripts/verify-*.ts; do
  case "$f" in *verify-mobile.ts) continue;; esac
  npx tsx --tsconfig tsconfig.app.json "$f" > /dev/null 2>&1 && echo "✅ $f" || echo "❌ $f"
done
npm run verify:mobile
npm run typecheck && npm run lint && npm run build
```
Expected: verify เดิม 28 ตัว + `verify-nav` เขียว, `verify:mobile` **M1–M6 เขียวหมด**

- [ ] **Step 2: ขับของจริงบนมือถือ**

`npm run dev` → กรอบ 390×844 → ไล่ทุกหน้า ทั้งโหมดสว่างและมืด:
- แถบล่างอยู่ครบ ไม่ทับเนื้อหาท้ายหน้า
- ปุ่มลอยกดแล้วฟอร์มรายจ่ายเปิด
- sheet "อื่นๆ" เปิด/ปิดได้ ไปหน้าที่เลือกได้
- ตารางเลื่อนแล้วคอลัมน์แรกตรึง
- **ไม่มีหน้าไหนเลื่อนแนวนอนได้**

- [ ] **Step 3: เอกสาร**

- `features.json` — เพิ่ม F47 ใน `phase_5`, `completionPercent: 67` (2/3), `progressSummary` → 55/55
- `CLAUDE.md` — Code Rules: เมนูทุกที่มาจาก `src/lib/nav.ts` (ห้าม hardcode รายการเมนูใน component); Dev Commands: เพิ่ม `npm run verify:mobile`
- `UXUI.md` — เพิ่มหัวข้อ mobile: แถบล่าง, ปุ่มลอย, 44px, ตรึงคอลัมน์

- [ ] **Step 4: commit**

```bash
git add features.json CLAUDE.md UXUI.md
git commit -m "docs: F47 mobile UX เสร็จ — แถบล่าง, ปุ่มลอย, 44px, ตรึงคอลัมน์"
```

---

## สิ่งที่ไม่ทำโดยตั้งใจ

- **ไม่แตะ `/report/:year`** — มันคือกระดาษ A4 สำหรับพิมพ์ ไม่ใช่หน้าจอ
- **ไม่ทำ PWA / offline / swipe gesture** — YAGNI
- **ไม่แตกตารางเป็นการ์ด** — ทำลายการเทียบข้ามแถว
- **ไม่แตะ layout เดสก์ท็อป** — M6 บังคับ
